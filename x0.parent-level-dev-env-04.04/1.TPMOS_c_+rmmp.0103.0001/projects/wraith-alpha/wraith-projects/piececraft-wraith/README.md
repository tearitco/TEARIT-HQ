# Piececraft Wraith

Internal Wraith validation project for `${game_map}`, z-level map audit, and `INTERACT` behavior.

Reference source:
- `projects/piececraft-3d`

This is not named `piececraft-3d` because it is a Wraith validation project, not the original project identity.

Initial scope:
- small z=0 map slice
- tile legend
- selected cell metadata
- ASCII/GL parity via Wraith receipts

See `ASSUMPTIONS.md` for the current design bets that need user correction.
