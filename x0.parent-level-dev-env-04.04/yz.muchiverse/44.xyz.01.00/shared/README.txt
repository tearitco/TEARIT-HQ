House-shared pointers (always in the zip).

*-ASSET-SOURCE-LOCATION.pdl and UNICODE/VIDEO SOURCE PDLs live here so
C can find them without #.NNEST_ASSETS (that tree is optional / not
always shipped). Each PDL's img_root / sample_mp4 / source_file still
names the clone on disk when it exists.

Do not put PNG/mod clones in this folder.
