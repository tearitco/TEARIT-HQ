# 02.LAN-nodes🌐.md — the two LAN compute nodes, live
> 2026-07-31. SSH access provided by user; ALL model compute runs on the
> LAN nodes — this box's CPU is never used for inference (hard constraint).

## Node 1 — MAC: `lfs.master@10.0.0.144` (pw: `1234`)
- Intel MacBook Pro, macOS 15.1.1, 12 cores, Radeon Pro 560X 4GB (Metal 3).
- python3 3.9.6, gcc/clang, miniconda3.
- Ollama already LAN-open: `http://10.0.0.144:11434` (bind 0.0.0.0 via
  `~/start_ollama.sh`; verified from this box with plain curl).
- Models: gemma3:270m, llama3-groq-tool-use(:latest/8b), llama2(+uncensored),
  nomic-embed-text, stable-code, llama3:latest.

## Node 2 — LINUX desktop: `jb@10.0.0.187` (pw: `root`)
- Dell Inspiron 3655, x86_64, 4 cores, 14GB RAM, NO GPU.
- Ubuntu-like; ollama 0.24.0 (snap). python3 3.10.12, gcc.
- WAS localhost-only → **I exposed it on the LAN** via
  `sudo snap set ollama host=0.0.0.0` + `sudo snap restart ollama`; now
  `http://10.0.0.187:11434` (verified from this box).
- **Pulled gemma3:270m per user directive. USER: "only use that one" on the
  Linux box** (slower for training, must use gemma, reliable Linux system).
  Other models still present (llama3-groq-tool-use:8b, qwen2.5-coder:7b,
  llama3:latest) — I did NOT delete them; can prune if user confirms.

## Codegen probe results (ran on the nodes, NOT here)
| node | model | syntax | playable? | verdict |
|---|---|---|---|---|
| Mac | stable-code | ✗ | — | fails |
| Mac | llama3:latest | ✓ | ✓ full game | **code-writer node** |
| Mac | gemma3:270m | ✓ | ✗ not a game | chat/tools only |
| Linux | gemma3:270m | ✗ | ✗ | chat/tools only |

Consistent conclusion: **gemma3:270m cannot write non-trivial code on ANY
node** (same incoherent pattern everywhere: `except random.random() for num
in range(7):`). This is fine — the agent's deterministic keyword pipeline
NEVER asks gemma for code; gemma's job is chat + tool intent matching.
llama3:latest (Mac) is the code-writer; it already produced + deployed a
playable game (`~/games/number_game.py` on the Mac).

## Fork plan (user: "run it on both mac and linux, fork them for tasks")
Proposed role split, per user's guidance (Linux=gemma-only, Mac=flexible):
- **Linux box (gemma3:270m ONLY)**: reliable, always-on Linux = the always-on
  agent/tool-router/chat responder; cheap. Slower training → keep it light.
- **Mac (gemma3:270m + llama3:latest + more)**: heavy lifting — code
  generation (llama3), iqabod training/distillation, bigger models.
- **This box**: orchestration/file work ONLY. No model compute.

## Status / next
- [x] Mac reached, LAN ollama confirmed, game deployed + verified.
- [x] Linux reached, ollama LAN-exposed, gemma3:270m pulled.
- [ ] IQABOD inference probe (on a node — ask user where).
- [ ] Prune Linux models to gemma-only if user wants (not done without ok).
- [ ] Distillation offload design (teacher=gemma on Linux/Mac, student=iqabod
      trained on Mac).
