# 00.session-001🌌.md — agent-convo session 1
> Created 2026-07-31 by the dev agent. Live working doc for the run-the-agent pass.

## The user's asks (verbatim intent, distilled)
1. Run the agent with ALL its features, using the J2 Testing Guide
   (`#.haiku+/tpmos-re-dox/_.0.aigent-testing-k3.txt`) as the playbook.
2. Ignore the gemini API connection for now — gemma/ollama only.
3. Assess: is the gemma harness usable for NON-TRIVIAL code creation?
   (Concretely: can it spin up a user-playable game on the LAN computer?)
4. Babysit vs set-and-forget — which is this agent today? What infra makes
   both easier?
5. State of iqabella (in-house DIY LLM). Can we test its inference power?
6. Offload training to Mac/LAN for distillation — NOT on this slow computer.
   (User offered Mac SSH info when needed.)

## Ground truth gathered this session
- **IQABOD** (not "iqabella" on disk) = real C transformer at
  `#.z.mirror_llm]z5]IQABOD🪞️+4/`: dim=16, n_layers=2, n_heads=4, seq_len=32,
  vocab=32 tokens (config.txt). Modules: attention/rope/feedforward/
  generation/vocab/backprop(mirror_model_v6)/feedback_tx. Has +x compiled
  binaries. Corpora: `corpuses/` + `curriculum/{guess_the_letter,guess_the_word}`.
  → **IQABELLA = the teacher/student DISTILLATION LOOP** (design only,
  documented in `$.zest-er-summary` sec. 4.2), not a separate model.
- Current iqabod output against real chat = mostly `<UNK>` (never trained on
  real chat). The mechanism isn't broken; the corpus is missing.
- The 4.2 plan: gemma-lan (teacher) + IQABELLA (student, provider_kind=iqabod)
  in ONE pal-chat-irc room, each its own agent session → harvest room to
  corpus (ops/harvest_room_to_corpus.c — designed, NEVER BUILT) → train via
  existing vocab_only + train pipeline → iterate → RL layer (human thumbs)
  → coding curriculum.
- Agent facts: model_after_tool default `no` (tool result IS the answer),
  gemma3:270m via ollama at localhost:11434/api/chat with deterministic
  keyword matching first, then LLM. Tool grammar: write/edit/read/run/search
  + list_dir. Known open bug: render-refresh freeze (jul-21-gemma-fix.txt) —
  FIRST task if touching the agent.
- Ollama 0.24.0 live: gemma3:latest (3.3GB), gemma3:270M, qwen2.5-coder:7b
  (4.7GB), llama3-groq-tool-use:8b, llama3.

## ⛔ HARD CONSTRAINT (user directive, 2026-07-31)
- **This computer is NOT a compute node.** Do NOT run local-model inference
  (ollama/gemma/IQABOD generation/training) on this box — it slows the CPU.
  Local models exist to serve OTHER machines on the LAN (the Mac).
- Never do "local model" tests here. If Mac LAN/SSH access is needed for
  inference/training (e.g. distillation offload, LAN-playable game), STOP and
  ask the user for the SSH info — don't touch this machine's CPU.
- Applies to: any model run, code-gen eval, IQABOD generate/train, gemma
  chat. This box does file/agent/harness work only.

## Done this session (evidence in `045.muchi-pal-agent🤖️+1/proof/`)
- [x] 5/5 scenario harnesses PASS (baseline, all KPIs):
      demo_list_dir_tool (strategy=A list_dir, KPI#2 no-Tool-halluc, KPI#3 order)
      demo_tool_hello_python (H1: write_file+exec_cmd, K1–K4)
      demo_tool_edit_book (H2: write/edit/read/search, K5–K8)
      demo_iqabod_chat (PITFALL-60 fix: relative api_url, real fork+generation;
        IQABOD raw output captured = <UNK>-riddled incoherence)
      demo_model_remember (dual-boot: gemma-lan first default → /model switch →
        remembered across session wipe)
- [x] Code-gen probe (qwen2.5-coder:7b only — gemma3:270m was incoherent;
      gemma3:latest aborted mid-test per the compute constraint): prompt
      "terminal game number, 7 guesses, 1-100" → SYNTAX OK + playable output
      ("Too low!"), markdown-fence wrapping needed stripping. **Only qwen2.5-coder:7b
      produced valid code — this box must NOT run it again; future runs go to the LAN.**

## Planned next steps (J2 style)
- [ ] Babysit vs set-and-forget assessment + infra gap list.
- [ ] LAN-playable game + distillation offload: STOP → get Mac SSH info from
      user first. No local inference.
- [ ] IQABOD inference probe: defer until LAN access (or user says otherwise).
