# Custom operations for civ-txt's own prisc+x program, modeled directly
# on @.apps/my-chara-txt's own default_op.txt (same menu-only, no-map
# shape for P1 - see that file for the real precedent this copies).
# Format: name type handler {description}
halt void builtin_halt {stop execution}
out void builtin_out {print register to stdout}
civ_menu_input void ops/+x/civ_menu_input.+x {piece.pdl METHOD dispatch for whichever civ-txt screen is current}
civ_compose_frame void ops/+x/civ_compose_frame.+x {render the current civ-txt screen into view.txt}
