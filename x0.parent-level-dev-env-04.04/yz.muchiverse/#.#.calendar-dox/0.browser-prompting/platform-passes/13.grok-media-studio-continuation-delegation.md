# 🎬🎨 Grok — 103.media-studio Continuation — delegation map + real decision framing

**Location confirmed:** `44.xyz.01.00/103.media-studio/` — subdirs: `100.tts-point-2-anything`,
`103.3d=blender-clone`, `103.daw`, `103.img-editor`, `103.vid-edit`, `shared/`, `XER/`.

## 🤔 The real decision you're weighing (worth deciding BEFORE prompting Grok)

**Option A — keep building out-of-house-spec (no navs/manager pattern yet):** faster short-term,
but every app built this way is a FUTURE merge cost, and it can't use the taskbar's real relay/
dispatch/render conventions (no HQ-menu integration, no ASCII-mirror-style testability, no
one-writer-rule state files other tools can read).

**Option B — merge into house spec now (navs, PIECE/MODULE/OS, layout+manager):** slower
per-feature, but every feature built this way is immediately taskbar-integrated and testable the
same way this session tested the taskbar itself (relay injection, `strip_input_history.txt`-style
audit trails).

**Honest guidance, not a dodge:** given you're ALSO about to hand this whole area off to browser-
window AI when tokens run out, **Option B compounds better** — house-spec code is exactly what the
`1.platform-primer-ALWAYS-ATTACH.md` context doc was built to describe in one upload, so future
browser prompts stay small. Out-of-spec code needs its OWN bespoke context doc every time, which
burns your limited "1 context file" slot on re-explaining a one-off shape instead of reusing the
house-wide primer.

**Pragmatic middle path**: let Grok keep building fast for the FIRST pass of a genuinely new
feature (prototyping), but require merge-to-house-spec as the SECOND pass before it's considered
done — never let unmerged, spec-divergent code accumulate past one feature's worth.

## 📎 What to attach (per media-studio sub-app)

| Slot | File |
|---|---|
| Context #1 | `1.platform-primer-ALWAYS-ATTACH.md` |
| Context #2 | This file's own decision framing above |
| Code | The specific sub-app's current source (ONE of `100.tts-point-2-anything`/`103.3d=blender-clone`/`103.daw`/`103.img-editor`/`103.vid-edit`) |

## ✍️ Prompt shape

```
CONTEXT: [platform-primer + this file]
CODE: [the ONE media-studio sub-app being worked on]
PROMPT:
  "This is currently built out-of-house-spec for speed. [Describe the new feature/fix]. Build it
  house-spec from the start (PIECE/MODULE/OS, file-backed state, thin manager) rather than adding
  to the existing spec-divergent pile - see the platform primer for the exact shape."
```
