# 🤝 Handoff — 2026-09-22

## 1. What landed this session (all on `claude`, pushed at the bottom of this doc)

**Taskbar performance fix** (`60fd7920`): `khtpm_taskbar_manager.c`'s
`ktb_find_live_pid_for_pal()` did a full `/proc` scan PER ENTITY inside
two hot loops (`livedesk_spawn_desk()`'s startup spawn loop and
`ktb_self_heal_active_desk_registry()`'s reconcile pass) — real root
cause of "taskbar takes a long time to populate"
(`bug_bounty.md` 2026-09-22 entry). Added `ktb_proc_snapshot()`/
`_find()`/`_free()`: one `/proc` scan per pass, in-memory lookup per
entity. Both call sites rewired; no leak risk (verified no early
`return` inside either loop body).

**AI-track design work** — `AI-TRACK-BRAINSTORM-QUESTIONS.md` gained
Questions 5–8, each dramatized as a NIGHT script (`#.#.calendar-dox/
1-1.HARNECIENT.SMOL/`, mp3s in that dir's `audio-book/`):
- **Q5 / (referenced by NIGHT_19)** — honest review of two external
  JEV architecture docs (`XO/LLMUD_CODE/8.0`/`8.1`): the Bank Layer is
  a real "mini-JEV" (Laplace-smoothed weighted lookups), useful
  synthesis, but the diagram overstates all four Banks as equally
  real when only Synonym/Behavior have real design work.
- **Q6 / NIGHT_19** — recorded, not yet acted on: token-savings as the
  FSM/RL/IRL loop's real success metric, parallel human-facing
  documentation of the same harnesses as a real product angle, wanting
  transparent KPIs. Deliberately left open/unresolved (episode framed
  that way on purpose).
- **Q7 / NIGHT_20** — real breakthrough: the `8.1` diagram has no
  training/corpus-chunk/attention path anywhere (Bank Layer is
  weight-only, Gemma is drawn frozen). That gap belongs to a new
  **Corpus/Training Layer**, feeding Famous LLM ("tomom") specifically
  — tomom is real, dormant, not missing. Also recorded: the "school"
  model (per-entity learners attending curriculum-based classes,
  teacher = user now/Gemma later, pass/fail gated), the goal of
  minimizing hardcoded game mechanics with even primitives staying
  trainable, and a universal bootstrap-in-Events policy.
- **Q8 / NIGHT_21** — a video-prep agent (working from Q7/NIGHT_20 to
  script a 20-min technical video) caught a real hole: Q7 never traced
  HOW tomom's learning changes actual gameplay. Answered with
  honesty-marked status: **event parameter tuning** (`.pdl` override +
  fallback read) is design-level and most concrete; **FSM path
  selection** (candidate Bank entry + the same reward-weighted
  promotion gate the Bank Layer already uses) is design-level;
  **Event generation** (DESCRIBE+deterministic-compiler vs direct
  emission) and **primitive-approximation shadowing** are marked
  UNBUILT — real open decisions, not invented. Concrete physics-
  training walkthrough included. See §2 below — this is the next real
  step.

**`dsr` desk work** (`7c510b89`, `2a84b3a2`) — the physical-layout
sandbox desk (`&.hq-apps/dsr/`'s own experimentation board, distinct
from the toy itself) renamed `dsr` → `dsr-dev`. First pass wrongly put
the school emoji (🏫) on the population/hotel buildings; corrected —
**hotels stay hotels** (entities live there), the **store adjacent to
each hotel** (`dsr_store_a4`/`dsr_store_b4`) is the school. Real
lesson recorded in-commit: a regenerated `sprite.csv` alone doesn't
change what's on screen — there's a **third cache layer**,
`pieces/registry/phymoji_assets/<entity>/voxels.csv`, generated once
from `sprite.csv` and reused forever unless deleted. All three
(glyph.txt → title/hash only, sprite.csv → real 2D sprite, phymoji
voxels.csv → the actual rendered asset) have to be kept in sync by
hand; there's no single "regenerate everything" entry point yet.

**Taskbar label naming** (`16710365`, `5ad198f6`) — `FILE:DESK`
naming went through two live iterations before landing:
`file:`/`desks:` → (wrong direction) `world:`/`desk:` → **final:
`book:`/`page:`** (a session is a `BOOK`, a desk is a `PAGE`). Real
code, not just vocabulary — `ktb_get_file_label()`/
`ktb_get_desks_label()` in `khtpm_taskbar_manager.c`. Verified live via
`dump_frame_png_op` against the actual strip window, not just the text
receipt.

**Real bug found + fixed along the way** (`16710365`):
`livedesk_splash.c`'s "BUILD FAILED" banner was a false positive on
nearly every build — it checked the `.build_failed.txt` dead-man's-
switch marker unconditionally from t=0 and latched permanently on
first sighting, but the marker is *supposed* to be present for the
whole build by design. Fixed to only check at the two real decision
points (all binaries fresh; timeout). **Note**: this fix needed one
extra rebuild cycle to take effect, since `build_khtpm_strip.sh` reuses
an already-built splash binary to show its own progress and only
rebuilds it later in the same run — already done, confirmed clean on
the next build after.

**Real process lesson, worth keeping**: pixels and receipts (text
state files like `strip_ui.txt`) are not the same evidence. A text
receipt updating does not prove the drawn pixels have caught up — a
frame dumped immediately after a khtpm restart can show stale content
from the previous window (same X11 window ID can persist across
restart; backing-store lag). Wait for an actual repaint before
trusting `dump_frame_png_op` output as proof.

## 2. Next real step

**Resolve Q8's open Event-generation decision and start the
Corpus/Training Layer design.** This is the one real architectural
fork left unresolved from tonight, and it's a prerequisite for any
actual item-3 implementation work:

- Decide, concretely (not just recommend): does tomom's learned Event
  output get expressed as (a) a DESCRIBE call + a deterministic
  compiler that turns natural language into a real compiled Event
  package (keeps the house's "model never routes/authors logic
  directly" law intact — Q8 8a's own recommendation), or (b) something
  else. (a) is currently the only shape consistent with existing house
  rules, but it hasn't been formally decided or scoped as real work.
- Once that's decided, sketch the actual Corpus/Training Layer (Q7):
  what a "class"/curriculum file looks like, how a Watch-Layer-
  observed sequence becomes a corpus chunk, what "retraining
  incrementally" means concretely for tomom.
- This naturally feeds the still-open item-3 KPI conversation (Q6) —
  a real design shape makes "what are we measuring" answerable instead
  of abstract.

Secondary, smaller, unrelated loose end: the `dsr` toy
(`&.hq-apps/dsr/`) still has no real spawn-target desk of its own —
its front door currently opens `dsr-dev` (the sandbox) as a stopgap.
Coding a real config from `dsr-dev`'s experiments into the toy to
spawn its own `dsr` desk is the explicitly-deferred next step for that
thread, whenever picked back up.

## 3. Known gaps / things NOT done

- No Corpus/Training Layer built. No school/curriculum file format
  defined. No KPI list drafted. All explicitly deliberate — see Q6/Q7/
  Q8's own "not yet done" sections.
- No single tool regenerates all three sprite-cache layers (glyph.txt/
  sprite.csv/phymoji voxels.csv) together — a real gap surfaced
  tonight, not fixed. Worth a real utility if entity re-theming happens
  again.
- The toy `dsr`'s own spawn-target desk (see §2 above).

## Grounding

`AI-TRACK-BRAINSTORM-QUESTIONS.md` (Q5–Q8), NIGHT_19–21 (`#.#.calendar-
dox/1-1.HARNECIENT.SMOL/`), `bug_bounty.md` (taskbar entry, now
closeable), `12.calendar/2026-09-20/2do.md`, `AGENTS.md` (commit/
verification discipline).
