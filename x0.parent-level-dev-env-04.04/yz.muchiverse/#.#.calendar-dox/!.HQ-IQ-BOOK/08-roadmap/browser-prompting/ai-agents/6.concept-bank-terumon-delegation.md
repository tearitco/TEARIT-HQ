# 🧠🐾 Concept Bank / tomom / terumon — delegation map

**Goal:** build the Concept Bank's real file format + validator — the
one piece everything else in this thread (tomom's real training,
meta-training/IRL, terumon learning) sits on top of and can't be
smoke-tested without. Written 2026-09-22, same day as the design.

**CORRECTED 2026-09-23:** the "zero code" line below is stale. The
bank is `44.xyz.01.00/&.widgits/concept-bank/`: masters `force`,
`motion`, `energy`; spoke `gravity_constant` (weight 0.60, points at
`force`); `ops/concept_edit_validate.c` and
`ops/+x/concept_edit_validate.+x` are present (commit `154316a5`).
Pass the concept-bank directory as argv 1, not `data/`. Hub-and-spoke
still holds: a spoke points at a master, never at another spoke.
`body_state.pdl` is still absent on the live ember pal (checked
2026-09-23). Combat numbers are a different file: `TERUMON` rows in
`#.desktop/db_hq_terumon.state.txt`, mirrored by each pal's
`stats.pdl` (level, exp, HP, atk/def/mat/mdf/agi/luk). Those rows are
not learnings. A learning is an Events-HQ event page.

**🚦 Start here, not with the whole thread**: `concept_bank/nodes/`,
`concept_bank/words/`, and `concept_edit_validate.+x` don't exist yet.
Zero code. The task below is the smallest real slice — hand-author one
relation, prove it validates.

**📐 If this touches any khtpm-family window/renderer code**: attach
`1.platform-primer-ALWAYS-ATTACH.md` (your default context slot, this
dir's own top rule) — it covers PIECE→MODULE→OS, layout+manager
always pairing, and the one-writer rule, which a browser AI has zero
memory of otherwise.

**🧪 Testing a live window this task touches**: drive it via its own
`#.desktop/entity_menu_history/<pid>.txt` relay file (real key/mouse
events, one per line) — **not** `xdotool`. Full convention:
`#.#.calendar-dox/1.^V-hq/_.0.aigent-testing-k9.txt`. Use
`dump_frame_png_op.+x` for real pixel proof once state looks right.

Don't ask a browser AI to build the whole loop in one shot; it won't fit the upload budget and you'll get
something unverifiable back.

## 📎 What to attach

| Slot | File |
|---|---|
| Context #1 | `design-docs/A-TEARIT-IS-ALL-YOU-NEED.md` §2.2-2.4 only (schema + validator spec) — **don't attach the whole doc**, it's long; paste just those two sections as text if your upload is code-only |
| Context #2 | `AI-TRACK-BRAINSTORM-QUESTIONS.md` Question 9, section 9b (the hub-and-spoke/normalization decisions — spoke is authoritative, master's mirror is derived-only) |
| Code | None yet — this is a from-scratch small C tool, no existing file to attach |

**Also relevant if the task is terumon-specific** (not the Concept
Bank itself): `terumon-dev/TERUMON-SPEC.md` — `learning_limits.pdl` is
a filter in *front of* the Concept Bank validator, not a second one.
Don't let a browser AI conflate the two schemas.

## ✍️ Prompt shape

```
CONTEXT: [paste A-TEARIT §2.2-2.4 + Q9 §9b as text, not file upload if long]
PROMPT:
  "Write a small, real C tool: concept_edit_validate.c. It reads a
  candidate edit record (see the EDIT format in the pasted context)
  and rejects it unless: (1) target/slot point at an already-existing
  master node file under concept_bank/nodes/, never a non-master; (2)
  delta is within a bounded range (name the range as a #define,
  suggest 0.0-0.2 given the pasted mlp_model.txt values that blew up
  into the thousands were the failure mode being avoided); (3) the
  record parses cleanly, reject on any malformed line, no partial
  apply. Also write the node/word file format itself as a real example
  pair (one master node, one spoke word pointing at it) matching the
  fixed-slot-record shape described in the pasted context. Keep it a
  real, small file-parsing C program in this house's own plain-C
  style (no external deps), not pseudocode."
```

**⚠️ Real, standing gap, confirmed 2026-09-22**: the entity system's
own per-entity-learner mechanism (which NIGHT_20 assumes exists) was
checked directly and is **not located anywhere in code** — it's a
design statement, not a build. If a task depends on "each entity
already has its own learner instance," verify that claim yourself
first; don't let a browser AI assume it's real because a NIGHT script
says so.

**Don't attach**: the full `A-TEARIT-IS-ALL-YOU-NEED.md` end to end —
it's a ~500-line spec covering the whole loop, FSM/GOAP reuse, the
Claude/Gemma division of labor, and the domain-breadth/"Attention Is
All You Need" argument. None of that is needed to build the validator.
Over-attaching burns your upload budget on sections the model doesn't
need for this specific task.
