# 🗺️ browser-prompting/ — START HERE

**Written 2026-08-18, direct request: "rephrase this prompt in ur own words + emoji heavy, more
indexed/organized + ur knowledge of current codebase & dox."** This is a **managerial delegation
map**, not a research report — each file below tells you what to attach and what to say, so a
browser-window AI (limited to ~3 uploads: 1 prompt + 1 context + 1-2 code files, returning ~1-2
pages of code) can get productive fast without re-deriving house context from scratch every time.

---

## 🎯 The constraint this whole directory is built around

You're moving to browser-window AI prompting once your CLI tokens run out this week. That AI:
- 📎 Can only take ~3 file uploads at once (1 prompt, 1 context, 1-2 code)
- 📄 Returns ~1-2 pages of code per round
- 🧠 Has ZERO memory of this house's conventions unless you tell it

**➡️ So every file below is small, dated, and numbered on purpose** — not because the topics are
small, but because YOUR upload budget is. Each one names the 2-3 real docs to attach + gives an
actual prompt template, not a research essay.

## 🪄 The one rule that saves you the most tokens

**Almost every task needs `1.platform-primer-ALWAYS-ATTACH.md` as your context slot.** It's the
one-file version of "how this house works" (PIECE→MODULE→OS, layout+manager always come as a
pair, the two parser families, input/relay/dispatch conventions, one-writer-rule). Attach it by
default; only swap it out when a topic file below says to add something instead of it.

---

## 📁 Index

### 🧱 Foundation (read/attach first)
| File | What it's for |
|---|---|
| `1.platform-primer-ALWAYS-ATTACH.md` | 🎫 your default context upload for almost everything |
| `2.house-shape-status-report.md` | 🏚️➡️🏡 honest answer to "is the house in shape?" — read yourself, don't necessarily upload |

### 🤖 AI agents / chat-hai (haiku-appropriate zone — see note below)
| File | Topic |
|---|---|
| `ai-agents/3.chat-hai-openhai-harnecient-delegation.md` | 💬 chat-hai UI, OpenRouter, Harnecient technique, "conversation agents with jobs" |
| `ai-agents/4.rmmv-event-scripting-ai-teaching-delegation.md` | 🎮 RMMV-style events + teaching weak local AI to build/nav them |
| `open-hai-free-ai/` | 🧪🆓 **its own directory now** — live-tested open-hai + free-tier OpenRouter findings, an informal `#.wassup-hai.txt` status note, real quota/rate-limit info, other free-API options. This thread is active and growing, that's why it got promoted out of `ai-agents/` — see Part 2 below. |

**🪙 Token-saving note**: per your own instruction, things you ask **Haiku** (not browser AI)
should stay scoped to files 3-4 above specifically — chat-hai/open-hai/Harnecient territory.
Everything else in this directory → browser AI.

### 🐛 Bugs, joystick, toys, GL migration
| File | Topic |
|---|---|
| `bugs-toys-gl/5.remaining-bugs-joystick-toys-delegation.md` | 🐛🕹️🧸 leftover bugs, joystick, more taskbar toys |
| `bugs-toys-gl/6.gl-to-x11-toolbar-piececraft-delegation.md` | 🖼️➡️🪟 piececraft-xyz GL→X11 (may already be partially done — check first!) |

### 🏗️ Architecture explainers (upload-as-context files for bigger design questions)
| File | Topic |
|---|---|
| `architecture-explainers/7.collaborative-entity-games-delegation.md` | 🤝🎲 multiple entities, same mechanic (roguelike/business-sim/farming) |
| `architecture-explainers/8.data-storage-ledger-delegation.md` | 📒🔒 how the backend ledger/shared-state system really works |
| `architecture-explainers/9.networking-13network-delegation.md` | 🌐📡 house-friendly networking apps + populating the `network` cell — **CORRECTED (see Part 2): the apps already exist, this is a finish-the-wiring job, not a from-scratch one** |
| `architecture-explainers/10.board-viewer-drag-drop-entities-delegation.md` | 🖱️🧩 drag-drop entity/event creation in board-viewer/muta |
| `architecture-explainers/11.palettes-toys-pals-delegation.md` | 🎨🧰 tile/sprite pickers as pals — **has a real open decision you need to make first** |

### 🚀 Grok's lanes
| File | Topic |
|---|---|
| `platform-passes/12.grok-windows-mac-compat-delegation.md` | 🪟🍎 Windows/Mac catch-up after Linux-focused drift, real cross-platform rules included |
| `platform-passes/13.grok-media-studio-continuation-delegation.md` | 🎬🎨 103.media-studio apps — **real decision framing on spec-now vs. spec-later included** |

### 🌱 Project starters (new work, from scratch)
| File | Topic |
|---|---|
| `project-starters/14.business-sim-game-starter-delegation.md` | 🏦🌾 your banks/stores/farmers business-sim plan |
| `project-starters/15.xyz-installer-dev-exploration-delegation.md` | 📦🧙 installer wizard — real file map of what's already there, plus a version-mismatch flag |
| `project-starters/16.calendar-notebook-hq-delegation.md` | 📅📓 calendar notebook app — **splits into 3 real sub-features, don't prompt them together** |

---

## 🧩 The "1 Apple app?" aside — quick answer, no dedicated file needed

You asked whether the calendar/notebook/etc. should be one app or ship separately. **Short
answer: ship all of them separately** — every one already needs "taskbar spine" (the real
PIECE/MODULE/OS + relay/dispatch shape) regardless, so bundling into one app buys you nothing
architecturally. The "framework shape document" you're picturing IS `1.platform-primer-
ALWAYS-ATTACH.md` — that's the narrow, reusable "widget shape" description, already written.
No new file needed for this specific question.

## 🆕 Part 2 (2026-08-18, same day, direct request: "add that 2 the 0 prompt doc as pt 2 also pls")

Everything below happened AFTER the original index above was written — corrections, a real live
AI test, and a direct priority call.

### 🎯 Priority recommendation (you asked "what should my priorities be, I'm going crazy")

Given your own long-term goal (migrate off paid tokens toward free/local AI), **validate the
free-AI pipeline FIRST**, ahead of onboarding/3D/events content work — not alongside it. Reasoning:
whether Qwen/free-tier-OpenRouter/local-Ollama can reliably do real agentic work is a GATE that
changes the shape of every other roadmap item. If it can't, everything else still routes through
browser-prompting or paid Sonnet regardless of which content topic you pick first. **This
validation is now DONE for a first real slice** — see `open-hai-free-ai/` below. Real verdict:
yes, usable now, for read-only/conversational work; write/edit/run still needs a human to approve
each action.

### ✅ Real correction: networking apps already exist

Original file 9 above wrongly said no networking apps exist. They do —
`041.pal-chain⛓️/`, `041.pal-forum👥️/`, `044.pal-chat-irc👥️+2/` (house root), each with real
`pal/`/`ops/`/`net/`/`button.sh` AND a `phase2-module-split-report.txt` (real prior house-alignment
work already done, not from scratch). File 9 has been rewritten with a real discovery-first prompt
shape instead of an assumed from-scratch conversion. **Lesson for you as the human router**: when
I don't have a directory listing directly in front of me, treat my "doesn't exist" claims as
unverified until you or I actually check — this one was wrong.

### 🧪🆓 open-hai + free-tier AI — validated live, has its OWN directory now (`open-hai-free-ai/`)

This grew into its own real, ongoing thread (not a one-off question), so it got promoted out of
`ai-agents/` into a dedicated directory, same reasoning as any topic that outgrows a single file:
- `open-hai-free-ai/#.wassup-hai.txt` — informal quick-reference note (your own wussup-doc style),
  covers real OpenRouter quota mechanics (the $10-lifetime-topup daily-cap threshold), other free
  APIs worth knowing (Google AI Studio, Groq, Mistral), and the real safety/gotcha findings below.
- `open-hai-free-ai/1.live-test-findings-2026-08-18.md` — the full writeup with real test
  transcripts: your OpenRouter key confirmed working, 2 new free models added (both files, manager
  AND render — they keep separate copies, real drift risk, kept in sync this pass), a real
  end-to-end conversation + a real `list_dir` tool-call execution confirmed live, the real
  read/list-vs-write/edit/run approval boundary, and a real path-handling gotcha (special
  characters like `&` in a path can get dropped by the model when it re-emits a tool call).
- Also found and fixed live: `&.widgits/open-hai/README.md` had a STALE bug warning (said nav
  digit-jump breaks past index 9 — the code already fixes this, ported from `chtpm_parser.c`'s own
  standard; only the doc never got updated). Corrected in place.

**Bottom line on "is open-hai + OpenRouter usable yet"**: yes, for real, confirmed live today —
not a guess, not a maybe. Use `open-hai-free-ai/` as your starting context for any future work in
this specific lane.

## 🆕 Part 3 (2026-09-02, house-path-migration + standards-refresh pass)

Real, dated update after checking this directory against current house state:

- **The house has moved.** Old path (`NNEST-11.17/.../44.xyz❤️‍🔥️00.17`)
  is now `/home/no/Desktop/github/work/NNEST-12.00/.../44.xyz.01.00`
  (both the ancestor path AND the house's own internal folder name
  changed — see `1.^V-hq/S1_HOUSE_PATH_MIGRATION.md`). If any file in
  this directory has an absolute path baked into a prompt template,
  re-check it before pasting — most don't, since the whole point of
  this directory is house-relative topic descriptions, not literal
  paths, but verify before trusting an old one.
- **`1.platform-primer-ALWAYS-ATTACH.md` (your default context file)
  was stale and has been corrected** (2026-09-02): it named the shared
  khtpm renderer binary by an old name (`khtpm_entity_menu_render.c`,
  since renamed `khtpm_core_render.c` and, as of 2026-09-01, merged
  with the taskbar strip parser too) and made **zero mention of
  `CENTROID_GOLD_STD.md`** — the mandatory architecture (adopted
  2026-08-31) for any NEW khtpm window/app. Both are now fixed in that
  file. **If you already have an older cached copy of the primer
  pasted somewhere, re-pull it** — this is exactly the kind of drift
  this directory exists to prevent.
- File 15 (`project-starters/15.xyz-installer-dev-exploration-
  delegation.md`) and the whole install/store/versioning topic are
  now under active, real, in-conversation discussion — see
  `2.SEPT/PHONDO_INSTALL_IDEAS.md`. Don't delegate an install-wizard
  task from file 15 without checking that doc first; the ground has
  shifted since file 15 was written (no fixed product name anymore —
  it's chosen per customer at ship time — plus real decisions on CLI-
  first bootstrap and store shape).

## 🧭 If a topic isn't covered yet

Add a new numbered file to whichever subdir fits (or a new subdir, following this same
`N.topic-name-delegation.md` shape), and add a row to the index above. Keep the same discipline:
2-3 attach-files max, an actual prompt template, not a research essay. This directory is only
useful as long as it stays smaller than the thing it's helping you avoid re-explaining every time.
