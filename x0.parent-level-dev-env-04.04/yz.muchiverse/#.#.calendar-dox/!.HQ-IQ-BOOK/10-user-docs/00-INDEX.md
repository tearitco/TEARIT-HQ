# 10 — User Docs

- `FEATURE-CATALOG.md` — **what the house can do right now**: every
  taskbar cell, HQ window, `@.apps/` toy and `&.widgits/` widget, how
  to open it, and an honest status (working / partial / placeholder /
  planned / legacy-CLI). Living doc — edit freely. Tilesets + events
  for the game clones: `08-roadmap/TILESETS-EVENTS-AND-GAME-CLONES.md`.
- `_.0.aigent-testing-u10.txt` — agent testing guide (key injection +
  frame review), how to test the history.txt-polling family vs. the
  raw-Xlib/XTest family.
- `PRESENTATION-VIDEO-PIPELINE.md` — how to make a paced, TTS-narrated
  proof MP4 from PNG dumps (`dump_frame_png_op` + `make_presentation_video.py`),
  the manifest format, and the honest status of browser media.
- `2026-09-18/WHY-DSR-WSR-PAUSED.md` — why Dustopia civ (WSR-CIV) and
  DSR menu-class are **paused, not cancelled**: inventory/robot-bag
  bootstrap first so those tracks have a real container.
