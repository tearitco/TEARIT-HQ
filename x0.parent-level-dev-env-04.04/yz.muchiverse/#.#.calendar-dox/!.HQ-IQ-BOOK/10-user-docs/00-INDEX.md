# 10 — User Docs

- `FEATURE-CATALOG.md` — **what the house can do right now**: every
  taskbar cell, HQ window, `@.apps/` toy and `&.widgits/` widget, how
  to open it, and an honest status (working / partial / placeholder /
  planned / legacy-CLI). Living doc — edit freely.
- `_.0.aigent-testing-u10.txt` — agent testing guide (key injection +
  frame review), how to test the history.txt-polling family vs. the
  raw-Xlib/XTest family.
