# news — external-facing announcements

Outbound, publish-ready copy: press releases, launch posts, and tweet
packs. Flat list of release files here; each release is a single file
that works for multiple outlets (classic release + dev post + a few
tweets per audience).

Reading order:
1. `PRESS-RELEASE-NBJS-DUK-2026-09-07.md` — the NB-JS engine / `duk`
   CLI launch (the only release so far).
2. `press-release-template.md` — TBD (template + facts-lock header to
   copy for the next release).

Rules (from `../00-INDEX.md`, apply here):
- Everything is **founder-owned and DRAFT** until the founder says
  otherwise. Agents draft; the founder decides.
- No invented pricing, legal posture, financials, or traction numbers.
- Every claim in a release should trace to a checked-in fact (the
  release file's "Facts lock" section does this); update both if the
  product changes.