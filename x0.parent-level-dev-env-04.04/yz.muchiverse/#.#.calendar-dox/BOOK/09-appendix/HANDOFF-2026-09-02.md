# 🤝 Handoff — 2026-09-02, end of session

**Read this FIRST if resuming cold.** Written because the user is
restarting the session in a new working directory. **New working
directory**: `/home/no/Desktop/github/work/NNEST-12.00/` (NOT the old
`NNEST-11.17/` emoji-path location — that still exists as an untouched
rollback copy, don't work in it by mistake).

## 1. The big-picture arc of today

1. **House path migration, both phases, DONE and pushed.** Moved off
   the emoji-laden ancestor path AND renamed the house's own internal
   emoji folders. See `09-appendix/S1_HOUSE_PATH_MIGRATION.md` §7 for
   full execution detail. Quick reference:
   | Old | New |
   |---|---|
   | `NNEST-11.17/` | `/home/no/Desktop/github/work/NNEST-12.00/` |
   | `44.xyz❤️‍🔥️00.17` | `44.xyz.01.00` |
   | `#.#.✅️.cal-user-sum` | `#.#.calendar-dox` |
2. **Full doc-house rebuild into a "book," DONE and pushed.** ~130
   scattered files across 5 locations condensed into
   `#.#.calendar-dox/BOOK/` (9 chapters + README TOC). See the book's
   own `README.md` for the chapter list. This IS the new canonical
   place for house-level docs going forward — don't let new docs
   sprawl back into loose files at `yz.muchiverse/` root or `44.xyz.
   01.00/` root.
3. **Reviewed a friend's network-browser work on two different
   branches/repos** (`tearitco/co-work` and `datascientist1994sbgrhsz/
   TEARIT-HQCGB`'s `c11-network-browser-2026-09-02` branch). Verdict:
   architecturally sound, converged with house standards independently
   (killed their own from-scratch renderer in favor of the real
   shared one). But the SECOND branch's own change-log document
   describes real C changes (manager, renderer, a JS eval op, a media
   op) that are **not actually present in the pushed branch** — only
   3 new shell scripts (currently unwired — manager doesn't handle
   their request verbs), a CSS tweak, and a concerning edit to the
   shared `livedesk_theme.pdl` runtime file. **Not merged.** Told the
   user to have the friend push the missing `.c` files before a real
   merge decision.
4. **Wrote 4 new book docs** answering real questions raised by that
   review:
   - `07-install-and-ship/SECURITY.md` — long-term security concerns
     for shipping to strangers (install trust, store supply-chain
     risk, network-fetch risk incl. a planned Duktape JS eval op,
     account/auth gaps).
   - `02-architecture/HTML-MEDIA-AND-SCRIPTING.md` — answers "does a
     khtpm browser need a separate HTML parser or new img/video tags?"
     (No — reuse the existing generic `sprite=`/`sprite.csv`
     mechanism; JS execution stays manager/op-side only, never in the
     shared renderer).
   - `07-install-and-ship/USER-JOURNEY-COMPLETION-GRAPH.md` — the full
     real user journey (first install → publishing a toy to the
     store) as a step-by-step completion graph, checked against real
     source per step. **Read this before doing any install/store
     work** — it has the real, current status of every step.
   - This file.
5. **Corrected a real mistake in my own doc**: initially wrote that
   avatar-creation no longer exists in the current house (only checked
   house-root maxdepth 1). User corrected me — it's real and
   substantial, at `0.user-pal👤️/01.avatar-creation👤️/`, 14 real op
   binaries (DNA/clone/token system), just on the legacy
   `chtpm_parser_pal` engine, not khtpm. Fixed in the completion graph
   doc.

## 2. What was IN PROGRESS when the session was interrupted

User asked for 3 unsupervised tasks while they stepped away:
1. **Login-menu label fix** — IN PROGRESS, NOT YET LIVE-VERIFIED.
2. Cursword onboarding FSM — NOT STARTED.
3. Avatar-creation khtpm-rework scoping doc (no code) — NOT STARTED.

### Login-menu label fix — real detail, needs finishing

**The bug (real, user-reported)**: "I only see logout, IS THERE LOGIN
LOGIC?" — real login logic exists (`userpal_login.c`, dispatch command
`user:switch:<id>`), but the USER taskbar menu (`livedesk_build_user_
menu()` in `*.monads/*.livedesk-taskbar/ops/khtpm_taskbar_manager.c`,
~line 3260-3345) labeled an existing user's row as just `<id> (<name>)`
with no indication that clicking it logs you in — looked like a
profile listing, not an action.

**The fix applied** (line ~3327-3330, already edited + rebuilt
successfully): prefix the label based on state —
- `is_cur` (this IS the currently logged-in user): `"* "` (unchanged
  from before)
- nobody logged in (`!cur_id[0]`): `"Login: "`
- someone else is logged in (`cur_id[0]` set, different user):
  `"Switch: "`

Dispatch command (`user:switch:<id>`) is UNCHANGED — this is a
label-only fix, zero behavior change, low risk.

**Build status**: `sh build_khtpm_strip.sh` ran clean, no errors.
**Live status**: `sh run_khtpm_strip.sh new` ran clean, all 7 entities
relaunched (confirmed via `ps aux` and `xwininfo -root -tree`).

**NOT YET VERIFIED**: tried to open the USER menu via `nav.sh hqcell
2` and screenshot the dropdown. Real complication:
- The nav.sh harness defaults to `$PWD` for house_root, and the shell
  tool's CWD kept resetting to the OLD (`NNEST-11.17/`) path between
  Bash calls — not a nav.sh bug, just needed `HOUSE=<new path>` passed
  explicitly. Confirmed working with the explicit env var.
- A window matching a dropdown's rough size (`0xa00004`, 312x288)
  appeared in `xwininfo` after the nav call, but `dump_frame_png_op.+x`
  failed against it with a real `X_GetImage BadMatch` error — the user
  also reported not seeing a dropdown visually. **Real, unresolved
  question**: was `0xa00004` actually the USER dropdown, or a stale/
  different window? Was the nav sequence even correct for opening
  cell 2's dropdown specifically (vs. some other cell)? This needs
  fresh, careful re-verification, not a repeat of the same failed
  attempt — check `xwininfo`'s Map State for that window before trying
  to screenshot it again, and confirm which real hqcell index `2` maps
  to (USER) by re-reading `nav.sh`'s own usage/cell-index convention
  or the taskbar's own cell layout, don't assume it's still correct
  after the day's other changes.

**Real next step**: before claiming this fix done, get a REAL
screenshot showing the "Login: "/"Switch: " prefix actually rendering
in the live dropdown — per this house's own hard verification rule
(`02-architecture` SKILLS.md content: "never say something is fixed
without a fresh build + a fresh live/headless run + real evidence").
Do not report this as fixed until that screenshot exists and has
actually been looked at.

## 3. Immediate next steps, in order

1. **Finish verifying the login-menu label fix** (see above) — this
   was mid-verification when interrupted, closest to done.
2. **Cursword onboarding FSM** — not started. See `USER-JOURNEY-
   COMPLETION-GRAPH.md` step 3 for the real design direction already
   agreed (simple FSM/BT, no AI API needed, drives real nav-injection
   into the USER cell's relay file, same mechanism this house already
   uses for testing).
3. **Avatar-creation khtpm-rework scoping doc** — not started, and per
   direct instruction this should be a SCOPING doc only, not code
   yet (the actual app is real, live-used, and a full rewrite is too
   big/risky to do unsupervised).
4. Continue install/store/CLI-bootstrap planning per user's own
   stated pace ("slow down and discuss... before we really get
   started") — do not unilaterally build the install script.

## 4. Standing reminders (don't re-learn these the hard way)

- **Always read `1.^V-hq`'s Tier-1 docs** — wait, that path moved.
  **Read `02-architecture/CENTROID_GOLD_STD.md` and this book's own
  `README.md`/chapter indexes before touching any khtpm renderer/
  manager code.** The old `1.^V-hq/INDEX.md` path is gone — its
  content is now spread across the book's chapters (see `README.md`).
- **Never claim something works without a fresh build + a real,
  looked-at screenshot or file-level proof.**
- **Shell CWD resets to the OLD path between Bash tool calls** in this
  environment — always `cd` explicitly or pass absolute paths/`HOUSE=`
  env vars rather than relying on a prior `cd` persisting.
- **The BOOK.7z stray archive** keeps getting auto-regenerated locally
  and swept into `git add -A` — it's gitignored now
  (`**/#.#.calendar-dox/BOOK.7z`), should no longer be an issue, but
  if it reappears in a `git status`, that's why.
- **Old house location** (`NNEST-11.17/...`) still exists as a
  rollback copy — don't confuse it with the real working copy, and
  don't delete it without asking (retirement timeline not yet
  discussed with the user).

## 5. Cross-references

- `#.#.calendar-dox/BOOK/README.md` — the book's own table of
  contents, start here for anything not covered above.
- `09-appendix/S1_HOUSE_PATH_MIGRATION.md` — full migration record.
- `07-install-and-ship/USER-JOURNEY-COMPLETION-GRAPH.md` — real
  install/store/onboarding status, step by step.
- `07-install-and-ship/SECURITY.md`, `02-architecture/HTML-MEDIA-
  AND-SCRIPTING.md` — today's two new architecture/security docs.
