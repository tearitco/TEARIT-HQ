FRAME_REPORT_20260821-0000_login-signup-jb-persistence
=======================================================
Topic: manual live test of symlink-migration Step 2 for 0.user-pal👤️/00.login-signup
(persist_session_state + HOUSE_ROOT export), driven with the human's real account `jb`.
Method per _.0.aigent-testing-k9.txt: file-injection family confirmed (tk_inject_key appends
strict `[TIMESTAMP] KEY_PRESSED: <code>` lines to the session's pieces/keyboard/history.txt),
frame evidence from pieces/display/current_frame.txt, isolation discipline before/after.

Test scope
----------
Real-user login flow end-to-end against the copy-based session strategy:
launch → logout stale state → type user id → Log In → kill → verify persistence at real roots.

Actions performed (all via harness ops, real injection path)
------------------------------------------------------------
1. Baseline captured: users/jb/profile.txt (uuid=0a9558a7-7c74-4358-833c-2d5b21edc421,
   display_name=JB Test) copied to /tmp/jb_profile_before.txt; zero stray processes confirmed.
2. Launched `bash button.sh run` (setsid, detached). Frame composed.
   NOTE: screen auto-restored "Logged in as: asw_a_2492048" - stale state from an earlier
   harness run; persistence works in the RESTORE direction too (unexpected but correct).
3. Injected: focus "Log Out" + Enter(13). Frame → "Not logged in."
4. Injected: focus "User ID" + Enter(13), 24x Backspace(127), typed "jb", Escape(27),
   focus "Log In" + Enter(13).
5. Frame after (~2s settle per k9 Rule 8):
      |  Logged in as: jb                                          |
      |  uuid: 0a9558a7-7c74-4358-833c-2d5b21edc421                |
      |  xyzfs: xyzfs/users/0a9558a7-7c74-4358-833c-2d5b21edc421   |
      |  Logged in as 'jb'.                                        |
   Session current_login.txt: current_user_id=jb, uuid match, logged_in_at updated.
6. `bash button.sh kill`; polled real-root current_login.txt until persist trap completed.

Verdicts
--------
PASS: launch + frame render (session-isolated copy dir under pieces/sessions/)
PASS: key injection ingestion (logout, field edit, typing, login all landed)
PASS: login as EXISTING real user jb (uuid matches pre-migration baseline exactly)
PASS: persist_session_state copied back on exit -
      REAL current_login.txt  -> current_user_id=jb / uuid=0a9558a7... / logged_in_at set
      REAL xyzfs/session.pdl  -> mode=logged_in, user_id=jb, display_name=JB Test
PASS: users/jb/profile.txt byte-identical to baseline (diff clean)
PASS: house-level xyzfs/users/0a9558a7.../ tree intact (home/, meta.txt)
PASS: zero stray processes after kill (bracket-pattern pgrep; only Chrome's own
      --type=renderer processes match, unrelated); session dir removed by trap

Notes / incidents
-----------------
- Bare digit '9' (Switch:jb row) did NOT land from Nav context - k9 pitfall #1 (mode/context
  sensitivity). Focus+Enter path worked every time. No app bug implicated.
- One tool-shell self-kill during cleanup: `bash button.sh kill` ran while my wrapper command
  line contained the literal strings kill's `pkill -f` matches -> pkill terminated my own
  shell (timeout, no output). State was unaffected; re-verified clean afterwards. Lesson for
  agents: never keep kill-target substrings in your OWN command line when invoking a kill
  action (k9 checklist item 6 applied to the caller, not just the target).
- Machine intentionally left LOGGED IN AS JB at the real roots (human's account; also
  overwrote stale asw_a test state left by the earlier account-switch scenario run).

SAFE TO PROCEED: yes - login-signup Step 2 verified live with real user data.
Next: human signoff -> delete .pre-symlink-swap backup -> proceed to 01.avatar-creation👤️.
