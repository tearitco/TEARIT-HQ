aug8-kilo-desk-2do.txt
======================

1. taskbar: replace "$" + "x" buttons with single "HQ" popup button
   - DONE 2026-08-08: HQ button at right edge, popup with $.restart + X.quit
   - config in #.desktop/livedesk_taskbar.pdl: hq_label, hq_menu_N_label, hq_menu_N_cmd
   - popup uses override_redirect, no grabs, dismisses on outside-click/Escape
   - files: &.widgits/livedesk-taskbar/ops/tp_taskbar.c
            #.desktop/livedesk_taskbar.pdl
            #.desktop/livedesk_shortcuts.pdl (emptied)

2. move "user:" tag to top middle-right of screen
   - DONE 2026-08-08: position now read from #.desktop/livedesk_taskbar.pdl
   - config keys: user_tag_x_offset (default 245), user_tag_y (default 0)
   - edit #.desktop/livedesk_taskbar.pdl to reposition, no recompile needed
   - files: &.widgits/livedesk-taskbar/ops/tp_taskbar.c
            #.desktop/livedesk_taskbar.pdl

3. add tiny deskpal sprite to its toolbar tab
   - current: tabs drawn at line 516 as text only: "[>] N. entity_name"
   - current: tp_desktop_window.c has draw_glyph() / draw_sprite() GL rendering
   - current: sprite data lives in <package_dir>/sprite.csv (generated by emoji_xtract)
   - plan: load sprite.csv for each entity at startup (or when tab appears)
   - plan: render 16x16 or 24x24 thumbnail on left side of each tab, before the text
   - plan: use X11 composite or GLX pixmap - either a tiny GL viewport per tab,
     or pre-render to an offscreen pixmap and XCopyArea into the tab strip
   - plan: fall back to text-only if sprite.csv missing or GL fails
   - files: &.widgits/livedesk-taskbar/ops/tp_taskbar.c
            (may need tiny GL helper - reuse draw_sprite() pattern)

4. top-left command strip: "file / play / pause / reset / db / plugins"
   - DIRECT INSTRUCTION 2026-08-08: new buttons like the HQ button, but pinned
     TOP-LEFT of the screen; they should OPEN when the toolbar opens and STAY
     OPEN like the toolbar/guest button (persistent, not a click-to-open popup).
    - buttons: file (with submenu: new-desk, save, save-as, load), desks,
      player (with submenu: play, pause, reset - DIRECT INSTRUCTION 2026-08-08:
      play/pause/reset all go under "player" as submenu), db, plugins
   - HQ button MOVES UP HERE TOO: DIRECT INSTRUCTION 2026-08-08 - HQ goes in
     this same top bar, FARTHEST LEFT (left of "file"). It leaves its current
     right-edge spot.
   - user/guest tag JOINS THE STRIP: DIRECT INSTRUCTION 2026-08-08 - the user
     tag (currently top-middle-right, "guest" when no user logged in) moves into
     this strip BEFORE "file". Tentative left-to-right order: HQ, user/guest,
     file, desks, player, db, plugins - user said "user before file",
     "lets see how that works out" (try it, adjust after seeing it live).
     - desks added DIRECT INSTRUCTION 2026-08-08: a "desks" option right after
       "file".
     - player added DIRECT INSTRUCTION 2026-08-08: play/pause/reset all go
       under ONE "player" button as its submenu.
   - HQ nav-index gap: DIRECT INSTRUCTION 2026-08-08 - HQ and its submenu items
     NEVER received a nav index; wire them into the house nav-index system
     (the shared live nav-claim pool / [>]/[^]/[ ] focus convention, §D.2) so
     keyboard navigation can reach HQ and every file/hq submenu row, same as
     every other real button/menu in the livedesk.
    - plan: extend tp_taskbar.c with a persistent top-left strip (same GL/X11
      window surface as the taskbar strip), rendered left of/above the main
      taskbar row; "file" opens a submenu like HQ's popup (override_redirect,
      no grabs, dismiss on outside-click/Escape) listing new-desk/save/save-as/load
    - plan: live in the same process as the taskbar so "opens when toolbar opens"
      is automatic; treat as a second strip, not a separate widget
    - plan: config keys in #.desktop/livedesk_taskbar.pdl (label + cmd per
      button, same hq_menu_N_* shape) so commands stay user-editable without
      recompiling; anchor any relative cmd to house_root (see !.HOUSE_STDS.md
      §F-18 - do NOT resolve from inherited CWD)
    - DONE 2026-08-08: persistent top-left strip window (same process, y=0,
      x from strip_x_offset, BAR_H tall). Verified live: HQ popup (3 rows incl.
      cancel), file submenu (4 rows), outside-click dismiss, nav claims
      claimed/released per popup open/close.
    - DONE 2026-08-08: user/guest tag is a real BUTTON in the strip (DIRECT
      INSTRUCTION "user will also be a button, can be used to change users
      later"); its command is strip_user_cmd (empty = no-op until wired).
    - DONE 2026-08-08: HQ nav-index gap - popup rows now claim real numbers
      from the shared pool (KIND=row, released on close); digit keys inside an
      open popup move [>] to the matching row, and armed-mode digit jump to a
      taskbar popup row focuses it in place (PATH==house_root short-circuit).
    - DONE 2026-08-08 (REAL FIX): HQ "cancel" button was invisible - pdl rows
      are 1-based (hq_menu_1..N) but the popup draws 0-based slots; load_hq_config
      now compacts filled slots down to 0-based before counting.
    - TODO 2026-08-08 (DIRECT INSTRUCTION): "hq (and the other submenus) should
      appear under toolbar offset if it was moved in config" - popups are children
      of RootWindow but open at strip-relative cells[i].x0; must be
      strip_x_offset + cells[i].x0 so the whole popup column slides with the strip.
    - TODO 2026-08-08 (DIRECT INSTRUCTION): "why none of them yet have nav
      numbers" - strip buttons (HQ/user/file/desks/player/db/plugins) show no
      nav index at all; each should display its number from the shared pool,
      like bottom-toolbar tabs do ("[>] N. label").
    - TODO 2026-08-08 (DIRECT INSTRUCTION): "nav numbers in hq are un navigatible"
      + "hq should have a nav number, and nav numbers grow, see chtpm/khtpm
      parser for the behavior" - popup rows must be navigable like the bottom
      toolbar / chtpm: Up/Down wrap the [>] cursor, digit keys accumulate and
      jump [>] to the matching global nav index within this menu's range
      (tp_desktop_window.c main(): popup_digit_accum / nav_base+i row labels),
      Enter activates the focused row, Escape closes.
    - files: &.widgits/livedesk-taskbar/ops/tp_taskbar.c
             #.desktop/livedesk_taskbar.pdl
    - ref: &tile-picker/ops/tp_desktop_window.c (chtpm popup keyboard handler,
      draw_context_menu nav_base+i labels, popup_digit_accum) - the exact
      navigation behavior to mirror in tp_taskbar.c's popups.
   - note: §F-18 pitfall (2026-08-08) - tp_taskbar now chdir(house_root) at
     startup; keep that invariant for every new command this strip runs.
