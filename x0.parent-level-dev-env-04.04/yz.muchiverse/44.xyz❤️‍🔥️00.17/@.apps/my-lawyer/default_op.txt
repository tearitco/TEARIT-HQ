# Custom operations for my-lawyer's own prisc+x program, modeled
# directly on my-biotech's own default_op.txt (same menu-only, no-map
# shape - see that file for the real precedent this copies).
# Format: name type handler {description}
halt void builtin_halt {stop execution}
out void builtin_out {print register to stdout}
mylawyer_menu_input void ops/+x/mylawyer_menu_input.+x {piece.pdl METHOD dispatch for whichever my-lawyer screen is current}
mylawyer_compose_frame void ops/+x/mylawyer_compose_frame.+x {render the current my-lawyer screen into view.txt}
