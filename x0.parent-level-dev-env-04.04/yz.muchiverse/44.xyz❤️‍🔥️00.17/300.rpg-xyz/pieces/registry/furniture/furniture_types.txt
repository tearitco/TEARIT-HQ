# mutaclsym furniture registry (global, non-located content)
# Format: glyph|id|name|walkable(0/1)|rgb_top(R,G,B)|unicode|rgb_top_emoji(R,G,B)
# Furniture is a SEPARATE layer from terrain (pieces/registry/terrain/),
# same split as real CDDA's furniture_and_terrain: a tile has a terrain
# glyph (ground/wall) *and* optionally a furniture glyph (what's sitting
# on that ground) at once. Not wired into map.txt/compose_frame.c/
# move_player.c yet - see mutaclsym/dox/01-cdda-architecture.md Phase 1.
# That needs a second per-tile layer in the map format (map.txt is
# currently terrain-only, single glyph per cell); adding this registry
# first, data-only, so the wiring step has real content to point at.
# rgb_top matches the same GL/RGB-mirror convention terrain_types.txt
# now uses - see that file's own header comment.
# unicode/rgb_top_emoji: same ASCII<->emoji toggle fields as
# terrain_types.txt - see that file's own header comment for the full
# reasoning (registry-field-per-row, not a second hardcoded table).
&|f_table|Table|0|110,70,40|🍽️|200,200,200
h|f_chair|Chair|1|170,130,90|🪑|139,90,43
b|f_bed|Bed|1|200,100,100|🛏️|180,80,80
k|f_counter|Counter|0|100,100,110|🗄️|100,100,110
s|f_sink|Sink|0|200,200,210|🚰|180,200,220
c|f_crate|Crate|0|150,120,80|📦|150,110,60
g|f_grave|Grave|1|130,130,140|🪦|90,90,100
