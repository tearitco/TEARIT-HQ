Test: Owner Report 2026-08-27 — Common Events (db-hq/events-hq)
Date: 2026-08-27
Archive path: /home/no/Desktop/🧩️Piecemark-IT/中.SP_00.00/🗡️.crswrd.media-archive/August-27/owner-report-20260827/
What it demonstrates:
- Common Events sidebar + panel in one live window (real launch via relay)
- Control Switch + Conditional Branch: true branch on switch ON, false branch on switch OFF, both confirmed by real compiled prisc bytecode execution
- Call Common Event compiles to a real OP call_event reference (nesting reference verified at compile level; see Status note below)
- Autorun fires exactly once per OFF->ON switch edge (ledger-confirmed)
- Parallel fires repeatedly while ON, gated by the real ~1s cooldown (ledger-confirmed)
- Changing only the trigger type leaves the switch name untouched, and vice versa
Test Evidence:
- Owner-Report-20260827.mp4 (6 frames, TTS narration, ~87s)
- snapshots/01..06_*.png (real dump_frame_png() captures for steps 1/2/3/6; real ledger text rendered to PNG for steps 4/5 — common_events_manager is a headless process with no window/PNG dump of its own)
- manifest.txt, REPRODUCE.md, owner_report_harness.sh (exact commands run), log.txt (full run transcript)
- Zero stray processes confirmed before and after (pgrep checked for khtpm_entity_menu_render.+x and common_events_manager.+x)
Status: Current. Deviation from the literal 6-step shot list: step 3 (nesting) was verified only 1 level deep at the compile level (IR -> OP call_event reference), not a live 3-level runtime call chain — flagged here rather than silently claimed as fully proven. Steps 4/5 used a real terminal/ledger text capture rendered to PNG (not the app's own dump_frame_png(), since common_events_manager.+x is headless) — this is real captured data, not fabricated, but a different capture route than the windowed-app convention; disclosed per the testing guide's honesty requirement.
