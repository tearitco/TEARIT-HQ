# 8.21.GROK-win.md — Windows livedesk (current)

**Updated:** 2026-08-21 (late)  
**Status:** Strip + login **jb**. Desk pals **visible** (sprites in colored squares). Bottom tabs register. Files/desks submenus **do not yet list session/desk rows**. Sprite **transparency** not restored (Win skipped XShape). TB-launched apps each need their own PE.  
**Dev model:** Linux primary. Windows = thin shims. See `#.WIN-COMPAT-RULE.md`.

---

## How to run (Windows)

Desktop: double-click `livedesk-start-button.exe` (also `livedesk-house-root.txt` beside it).

Or from house `$.crypts/`:

```powershell
powershell -ExecutionPolicy Bypass -File .\button.ps1 run
powershell -ExecutionPolicy Bypass -File .\button.ps1 kill
powershell -ExecutionPolicy Bypass -File .\button.ps1 compile
powershell -ExecutionPolicy Bypass -File .\button.ps1 install-desktop
```

MSYS2 MinGW: `C:\msys64\mingw64\bin\gcc.exe`.

---

## What is true now (do not follow 08-06 stub docs)

| Piece | Linux | Windows |
|---|---|---|
| Autostart | `$.crypts/button.sh` + `crypt_autostart.+x` | `button.ps1` + `crypt_autostart.exe` (`-mwindows`) |
| PDL | `*.monads/...` spelling, house-relative pal args | same PDL; `*.foo` → `_.foo` at resolve time (`*` is illegal on NTFS) |
| Taskbar parser | `khtpm_strip_parser.c` + Xlib/Xft | **same `.c`** + `khtpm_strip_x11_win.c` shim → `khtpm_strip_parser.exe` |
| Taskbar manager | `khtpm_taskbar_manager.c` + `_main.c` | **same sources** → `khtpm_taskbar_manager_main.exe` (`-mwindows`) |
| Layout | `khtpm_strip_header.chtpm` / `_bottom.chtpm` | same files (`[>] 1. HQ` etc.) |
| Entity window | `tp_desktop_window_rgb.c` in `*.monads/*.livedesk-taskbar/ops/` | **same `.c` + X11 shim** → `tp_desktop_window_rgb.exe`. Live: self/asa sprites inside opaque squares |
| On-disk monad names | `*.monads` / `*.livedesk-taskbar` / `*.muchi-pet` | `_.monads` / `_.livedesk-taskbar` / `_.muchi-pet` (**alias only**; do not rewrite PDL) |

**Do not resurrect:** `tp_taskbar.c`, `tp_taskbar_win.c`, `khtpm_taskbar_plat_win.c`, tile-picker `khtpm_core`/`plat_win` as the taskbar. Archived in `LEGACY-ARCHIVE-20260811.zip`.

**Do not use** `khtpm_strip_parser_win.c` as the product bar (early GDI sketch only).

---

## Windows shims (taskbar)

File: `_.monads/_.livedesk-taskbar/ops/khtpm_strip_x11_win.c` (+ `.h`).

- Xlib/Xft subset so the Linux parser compiles.
- Star-alias in parser `path_join2` (`*.` → `_.`).
- `CREATE_NO_WINDOW` + Toolhelp kill (no `taskkill` cmd flashes).
- `SetProcessDPIAware` + clamp to **primary work area**.
- Header pixmap scaled into the visible window if Linux `strlen*8` width is too wide.
- **Unbias Linux desktop pad:** PDL `strip_y_offset` ~50 (GNOME) → y=0; popup under header; bottom bar flush above the Windows taskbar. Do not change the PDL (Linux still needs 50).

`crypt_autostart.c`: `make_rel_to_house` strips `/yz.muchiverse/<house>/` and `xyzfs/`; Win-only `win_star_alias`.

---

## Next to do (ordered)

Do these in order. Linux sources stay canonical; Win = shim / `#ifdef`, not a second product.

### 1. Entity windows + livedesk login (checkpoint 2026-08-21 late)

Desk pals are **`tp_desktop_window_rgb.exe` + pal dir**, not `muchi-pet/button.sh`. Autostart already does that. `01.muchi-pals` has `button.ps1` (egg app). Monad **`_.monads\_.muchi-pet`** is the Win spelling of Linux `*.monads/*.muchi-pet` — resolve with `win_star_alias`; **never rename PDL to `_`**.

- [x] Compile shared `tp_desktop_window_rgb.c` + X11 shim (gcc `-o` via `%TEMP%` then copy; emoji `-o` into house fails).
- [x] Autostart pal argv **house-relative** `xyzfs/users/<uuid>/home/livedesk/pals/<name>` (do not rewrite PDL).
- [x] Username **jb** from `0.user-pal*/00.login-signup/current_login.txt` (`current_user_id=`). Not hardcoded. Not `userpal_whoami.+x` (bash).
- [x] USER **menu** lists `users/*/profile.txt` via `FindFirstFileW` (ANSI `opendir` missed the emoji monad).
- [x] Pals **visible**: drop `WS_EX_LAYERED`; skip `XShapeCombineMask` (empty RGN hid tiles). First-frame `Expose`. Clamp to work area.
- [x] Bottom-bar tabs: `livedesk_open.txt` **append** on Win (`rename` raced). User saw entity tabs + self/asa sprites.
- [x] HQ Cancel / empty cmd: no `system(pgrep)` / no bash flash.
- [x] `desk_01.pdl` empty → spawn **office.pdl** on Win.
- [ ] **Sprite transparency** like Linux (colored square behind PNG). Restore shape/alpha without empty RGN.
- [ ] All pals on desk (user confirmed self + asa; others may still sit at edges).
- [ ] RMB / drag polish.
- [ ] Do **not** point autostart or TB pals at tile-picker `tp_desktop_window.exe` or `muchi-pals\button.ps1`.

### 2. Files + desks submenus (next — user confirmed not loading)

Linux: **file** = sessions under `xyzfs/users/<uuid>/home/livedesk/sessions/`; **desks** = `…/sessions/<id>/desks/*.pdl`. Same manager C; Win must `FindFirstFileW` / `ktb_fopen` like USER.

- [ ] **file** submenu: real session rows (`s1`…), not only static new/save/load/Cancel.
- [ ] **desks** submenu: `desk_02` / `office` / … with entity counts; load via `livedesk_spawn_desk` + rgb `.exe`.
- [ ] Confirm `livedesk_sessions_root` uses login uuid `0a9558a7-…` (jb), not first `xyzfs/users/*`.
- [ ] Do not rewrite empty `desk_01.pdl`; keep preferring office until a real save.

### 2b. Entity polish

- [x] RMB **legacy Xlib menu** on Win (all pals have `menu.chtpm`; Linux would fork `khtpm_entity_menu_render` which has **no Win PE** — that path was destroying the menu and showing nothing).
- [ ] Transparent tile — **PARKED**. Last tries (`SetWindowRgn`, `UpdateLayeredWindow`) hid/killed pals. Rollback zip **before** retry: `44.xyz-00.17-win-2026-08-21-ctxmenu.7z`. Force-show does **not** make those APIs safe. Do **db-hq / open-hai / events-hq** first.
- [ ] Drag + `desktop_pos.txt`.
- [ ] Dir → Explorer (Win `ShellExecute` on `xdg-open` METHOD). Other METHOD rows still bash.

### 2c. Book-stack bible (not the desk sprite)

The **desk tile** is `pals/book-stack/sprite.csv` (stack glyph). It is **not** the bible library.

**Read** (meta.pdl / menu.chtpm) is Linux-only:

`find $H -name '101.mutaclsym*' …/prisc+x` + `*monads/*book-stack/pieces/reader/event_pkg/pages/page_1/event.pal`

That is a **CHTPM/prisc** session, not rgb. On Win: no bash `find`, no `prisc+x` ELF, `*.monads` → `_.monads\_.book-stack`.

**Bible files for this checkout:**  
`C:\Users\jbro8\OneDrive\Desktop\assets-4win\bible-ench.twins+ai]b2`  
(`!.bible.DeathNote.ch]+x`, `bible.ch2en.ran`, …)

Do **not** rewrite METHOD PDL. Shim later: `prisc.exe` + star-alias reader pal + that assets dir (or `asset_path_win=` only if converting a cover PNG to `sprite.csv` via `tp_asset_to_sprite.exe`, which is also not built).

**Dir** on book-stack = `xdg-open $package_dir` → Explorer of the pal folder, **not** `assets-4win`.

### 2d. Events / events-hq (entity RMB “Events”)

ava/asa METHOD (do not rewrite PDL):

| Row | Linux | Win |
|---|---|---|
| **Events** | `"$1/&.widgits/events-hq/button.sh" "$0" "$1"` | Same family as db-hq: `khtpm_*` X11 renderer. **No PE yet.** Manager/entity must CreateProcess the exe when built — **not** `system(sh)`. |
| **Events (ez)** | `event-ez/button.sh run-widget` + `EZ_PKG_DIR=…/asa-&-ava/pieces/…/event_pkg` | Separate CHTPM/freeglut stack. See `#.WIN-CONVERSION-STATUS.md` §2. |
| **Events (mock)** | `event-editor/gl_mock/button.sh` | later |
| **Play** | `prisc+x` + `event.pal` | same as book-stack Read |

**Order (non-destructive):** book-stack Dir/Explorer (done) → **db-hq** include-shim compile (`khtpm_hq_render.c` + x11_win; failed on `sys/wait.h`) → **open-hai** (`sys/select.h`) → **events-hq** (same khtpm pattern as db-hq) → then transparency from **ctxmenu.7z** only.

Hai-hq = Open h-ai (`khtpm_open_hai_render`) and/or Chat-h-ai (`khtpm_entity_menu_render` + `chat-hai.chtpm`). Spawn is already wired in the manager; **gcc of those .c files is the remaining work.**

### 3. Path hygiene (house, both OS)

- [ ] No new `/home/no/...` in PDL. Keep `*.monads` spelling; alias only on Win.
- [ ] Grep leftover abs METHOD lines (ava/asa Events ez is the known one).
- [ ] UTF-8 **no BOM** on PDL/state. `-LiteralPath` in PowerShell.

### 4. Events (ez) — after entities RMB works

Not a KHTPM rewrite. See `#.WIN-CONVERSION-STATUS.md` §2.

- [ ] Smoke `event-ez/button.ps1 run` **outside** the desk first (need Win freeglut `gl_mirror.exe`, copy from `014.wsr-pal*` if needed).
- [ ] Relative METHOD / `open_event_ez.ps1` for ava/asa.
- [ ] Do not re-break `event-ez/button.sh`.

### 5. Every program the taskbar references (own Win pass)

Clicking a strip row only **spawns** a child. Linux uses `setsid nohup sh button.sh`. That is a no-op on Win until **that app** has a PE + shim (same rule as the strip). Do not pretend the strip “includes” chat-hai.

| TB cell / command | Linux launch | Win status (2026-08-21 late) |
|---|---|---|
| file / desks | manager lists sessions/desks | **labels show; lists not loaded yet** — next |
| pals (HQ) | `livedesk:pal:` + rgb | rgb spawn wired; menu after login-root FindFirst |
| h-ai Chat | `khtpm_entity_menu_render.+x` + `chat-hai.chtpm` | CreateProcess wired; **exe not built** (`sys/wait.h`) |
| Open h-ai | `&.widgits/open-hai/.../khtpm_open_hai_render.+x` | CreateProcess wired; **exe not built** (`sys/select.h`) |
| db-hq | `khtpm_hq_render.+x` | CreateProcess wired; **exe not built** |
| settings / stats / cli | bash launchers | silent no-op until PE |
| Events (ez) | event-ez button.sh | see §4 |
| user whoami / login / logout | `userpal_*.+x` | **label** from `current_login.txt`; switch/logout **not** spawned yet |
| `_.monads\_.muchi-pet` | Linux `*.monads/*.muchi-pet` | **star-alias**; own `button.ps1` later. Not desk rgb |

- [ ] Inventory remaining `system("setsid nohup sh …")` in `khtpm_taskbar_manager.c`.
- [ ] One app at a time: compile **that** binary + X11 shim (or its `button.ps1`), then manager CreateProcess. No bash popup.
- [ ] After files/desks lists: open-hai + db-hq include shims, then `_.muchi-pet`.

### 6. Other CHTPM apps (one at a time, later)

Queue in `#.WIN-CONVERSION-STATUS.md`: board-viewer live GL, 101.mutaclsym re-smoke, yahoo-app, event-ez full GL. Doctrine: `PRISC_PROJECT_ROOT=.`, name-only kill, no abs emoji `fopen`.

### 7. Do not do

- Resurrect `tp_taskbar*` / `khtpm_taskbar_plat_*` / tile-picker `khtpm_core` as the bar.
- Ship `khtpm_strip_parser_win.c` as the product strip.
- Rewrite Linux PDL to `_.monads`.
- `Get-Process | … $_.Path` or `system(taskkill)`.
- New symlinks.

### 8. Backup

- [x] 7z before entity pass: `yz.muchiverse/44.xyz-00.17-win-2026-08-21.7z` (~14 MB).
- [x] 7z after pals-visible checkpoint: `yz.muchiverse/44.xyz-00.17-win-2026-08-21-pals.7z`.
- [x] 7z after restart-spawn fix (no `SW_HIDE` on pal/strip): `yz.muchiverse/44.xyz-00.17-win-2026-08-21-pals-restart.7z`.

---

## Pitfalls that bit this session

- Windows cannot create dirs named `*.monads`. Alias at resolve; do not rewrite Linux PDL to `_.monads`.
- `New-Item -Path` glob-expands `*`. Always `-LiteralPath`.
- PowerShell 5 `Set-Content -Encoding utf8` writes a **BOM**. PDL: UTF-8 no BOM.
- Never `Get-Process | … $_.Path` (hangs). Kill by name.
- `system("taskkill…")` flashes a console per call. Use Toolhelp.
- Console-subsystem `.exe` flashes on spawn. Compile livedesk bins `-mwindows`; `CREATE_NO_WINDOW` on CreateProcess.
- `$.crypts` inside a PowerShell `-File` string expands as `$crypts`. Desktop starter launches `crypt_autostart.exe` directly.
- Agent/job launch can kill children unless `CREATE_BREAKAWAY_FROM_JOB`.
- Linux `strip_y_offset=50` is GNOME, not a Win inset.
- Strip **guest** ≠ OS user: livedesk login is `current_login.txt` (`jb`). `userpal_whoami.+x` is bash.
- Empty `XShapeCombineMask` RGN on Win hides entity windows even while PIDs exist.
- First `xyzfs/users/*` with a pals dir is **not** always jb (many guest uuids).
- `WS_EX_LAYERED` without a layered update → 64px tiles stay fully transparent.
- Win `rename()` cannot replace `livedesk_open.txt` → tab registry lost (append instead).
- `_.monads\_.muchi-pet` is **not** a different product; `*` is illegal on NTFS.
- Desktop restart vs agent `Start-Process`: `crypt_autostart` used `SW_HIDE`+`CREATE_NO_WINDOW`; pals inherited it and never appeared (tabs pruned). Spawn GUI without those flags.

---

## Appendix — morning 2026-08-21 failure (fixed)

`button.ps1 run` then: illegal `*` in `New-Item`; compile pointed at archived tile-picker taskbar files; `autostart.pdl` pal args were `/home/no/...`; `CreateProcessW` **123** (`*` in exe path). All addressed above.

`!.linux-absolute-FIXME-a6.txt` is still missing from this checkout; relative PDL is the replacement for autostart.

---

*Livedesk taskbar Win vertical: 2026-08-21. Entity rgb is the next coding pass.*
