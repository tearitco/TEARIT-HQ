# Custom operations for user-pal's own prisc+x program, modeled
# directly on pal-forum's own default_op.txt (same menu-only, no-map
# shape - see that file for the real precedent this copies).
# Format: name type handler {description}
halt void builtin_halt {stop execution}
out void builtin_out {print register to stdout}
userpal_menu_input void ops/+x/userpal_menu_input.+x {piece.pdl METHOD dispatch for user-pal's own login screen}
userpal_compose_frame void ops/+x/userpal_compose_frame.+x {render user-pal's own login screen into view.txt}
