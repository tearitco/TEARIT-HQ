# Parallel Agent Prompt — Palette/Tileset Picker

**Purpose:** hand this to a separate agent session to build the palette/tileset picker in parallel
with event-runtime work, without file/scope collisions.

**⚠️ REAL RESULT, 2026-08-12 — first attempt burned its entire usage budget on research, produced
zero deliverable.** Handed to a separate agent (transcript: `a12.tile-research.md`, 7343 lines). It
read `INDEX.md`/`EVENT_AI_VISION.md` as instructed, then went deep into unrelated exploratory reading
(`tp_desktop_window.c` raw-X11 internals, `wraith-alpha`, the `aomorai-editor` blueprint, unrelated
CPU-throttling troubleshooting) before running out of paid-model usage and getting compacted to a free
fallback model mid-task. No `&.widgits/palette-picker/` directory, no `PALETTE_SYSTEM.md`, no code was
ever produced. **Verdict: this prompt as written is too large/open-ended for one agent pass** — it
invites broad research (RPG Maker animation format + stable-ID architecture + migration-tool design +
full house-convention onboarding) with no small checkpoint to stop at.

**Before reusing this prompt:** cut it down to ONE small, mechanical, near-zero-research first slice
(e.g. "locate the real TSOTS tileset PNGs and slice one into a grid of individual tile thumbnail
files, no picker UI, no animation handling, no architecture decisions yet") and let that succeed
end-to-end before handing over anything resembling the full original scope below. Don't hand the full
prompt to a fresh agent expecting it to self-scope down — it won't, based on this evidence.

---

You're building the first version of a palette/tileset picker for a game engine (a project called
"livedesk" inside a large house-style codebase). Read this whole prompt before touching anything —
it defines hard boundaries meant to keep your work from colliding with another agent working on the
event runtime in parallel.

## Context you need

This house is mid-migration from scattered "dev folder" locations (`@.apps/`, `*.monads/<game>/`)
toward a shared, session-based model. A parallel agent is working on the event runtime
(`*.monads/*.livedesk-taskbar/`, `&.widgits/event-ez/`, `xyzfs/bin/muchi-pet/`) — DO NOT touch any of
those paths, or `khtpm_taskbar_manager.c` / `khtpm_strip_header.chtpm` / `khtpm_strip_parser.c`, even
to "wire in" your palette work. Integration with the taskbar's existing "palettes" header cell
(currently an inert placeholder, `onClick="ACTIVATE:6"` with no submenu, in
`khtpm_strip_header.chtpm`) is a deliberate later step, not part of this task.

Read these two docs first, in this order (both under
`.../yz.muchiverse/#.#.✅️.cal-user-sum/au11-hq/`):
1. `INDEX.md` — orientation, house conventions, standing rules
2. `EVENT_AI_VISION.md`, section "5. Palettes" (including the "Revised, 2026-08-12" subsection) — the
   actual real requirements for this task, including where the real tile assets live and the
   animation/swap-out requirements below

## The task

Build a standalone palette/tileset picker tool, following the exact same STRUCTURAL PATTERN as the
existing "event-ez" widget (`&.widgits/event-ez/` — study its `button.sh`, `ops/` dir, and `.chtpm`
layout files as your template: own launcher script, own compiled ops binaries, own chtpm-based UI).
Put your new tool at `&.widgits/palette-picker/` (or similarly named new directory).

### Real asset source — locate it fresh, don't guess the path
A real RPG Maker MV tile asset dump (project "TSOTS") exists on a SEPARATE MOUNTED DRIVE at roughly:
`/media/no/b7ced73c-5231-4462-b98d-64e38fe2df9e/home/jbez/Desktop/XVS.🕹️xoGames-STICKY]🕹️/
RMMV_TSOTS]LINUX=elf.../` — the exact folder names are full of unicode variation selectors/emoji that
don't survive manual retyping reliably (I hit this myself). Use `find ... -iname "*TSOTS*"` or similar
to locate the real `www/img/` (or `www/img/tilesets/`) subfolder yourself rather than trusting any
literal path string, including this one. There's also a `TSOTS_SECRETS0000.zip` nearby that may need
extracting.

### What it needs to do (v1 scope)
1. Let a user browse the real RPG Maker tileset images and select a tile.
2. Let the user place a selected tile onto a NEW tile-grid layer for a desk. Desks currently ONLY
   store entity positions (see `xyzfs/users/*/home/livedesk/sessions/*/desks/*.pdl` — format is
   `DESK | <entity> | <path> | x | y | col | row | glyph | nav`), with NO background tile-grid concept
   at all yet. You are introducing this for the first time. Propose your own storage format for the
   tile grid (document your reasoning) — but see the stable-ID requirement below, it constrains this.
3. **Animated tiles must render correctly, including live on a desk after "Play."** RPG Maker MV
   tilesets encode certain animated tiles (classically water/waterfall) as multi-frame strips within
   the tileset image at specific sheet positions, cycling at a fixed rate. Research RPG Maker MV's
   real autotile/animation convention (don't guess the frame layout) and make both the picker UI and
   whatever renders tiles on a live desk understand it.
4. **Architect for future substitution, don't just use RPG Maker tiles blindly.** Every tile must be
   referenced by a STABLE ID/slot in your data model, independent of which literal image currently
   backs it, with its exact pixel dimensions recorded. The goal: once a generative tileset pipeline
   exists (see below), a same-size generated tile can drop into an existing slot with zero changes to
   anything that references that tile (desk grids, any future tile-triggered events, etc.). Also
   design (build a skeleton of, if time allows) an actual migration tool that could later re-point a
   slot from an RPG Maker source image to a generated one — don't just leave a comment saying this
   should be possible.

### Real reference material (verified to exist, read before designing)
- `201.rpg-maker-clone/src/tileset.c` + `.h` — a real, already-built tileset rendering/slicing engine
  in this house. Study its approach before inventing your own. Confirm whether it bundles any actual
  tile PNGs of its own (separate question from the real TSOTS asset dump above).
- `#.ref/2.diffusion.fast]🖼️🖌️🎨️]c4]cl++/` — a real, working C+OpenCL image diffusion pipeline
  (currently does conditional enhancement, not yet unconditional generation — its own
  `#.image_generation_guide.txt` documents the path to true generation). This IS the eventual
  generative tileset source your stable-ID/slot design needs to accommodate later. Don't build on
  this now — just make sure your data model doesn't block it later.

### Explicitly OUT OF SCOPE for this task (don't start these)
- Actually building/finishing the ML/diffusion generator itself. Architect for it, don't build it.
- Wiring into khtpm's taskbar UI (see boundary note above).
- Anything in the event runtime.

## House conventions you must follow

1. **Testing is relay/injection-only, never direct CLI calls to your own binaries as "the test."**
   Build your own file-based relay (mirroring event-ez/khtpm — a plain text file your tool polls, one
   command per line), documented the way `nav.sh`'s own header comment documents its contract.
2. **Check for an existing pattern before inventing a new one.** Local chtpm usage first, then the
   genesis reference at `1.TPMOS_c_+rmmp.0103.0001/` if nothing local fits. Hard standing rule here.
3. **Own your test artifacts.** Create your own account if needed via livedesk's USER cell using the
   real relay-based flow documented in `au11-hq/USER_CREATION.md` (not the CLI directly), and store
   your own harnesses under your own `xyzfs/users/<your_account>/harnesses/`.
4. **Document as you go, real findings only.** Create `au11-hq/PALETTE_SYSTEM.md` — follow the
   structure/tone of `au11-hq/USER_CREATION.md` or `au11-hq/EVENTS_RUNTIME.md` (status line at top,
   real bugs with root cause + fix, test log with actual commands and actual output).
5. **Don't guess at things you can verify.** Unsure about the animation frame layout, an existing
   format, or a design decision that isn't yours to make — stop and document the question.
6. **When done or handing off:** add ONE line to `au11-hq/INDEX.md`'s document-roles table for your
   new doc, and append (don't edit existing lines) a short dated entry to the bottom of
   `au11-hq/2do-au11.txt`'s Progress Log. Keep these edits small and additive — another agent may be
   editing these same shared files concurrently.

Start by locating the real TSOTS asset folder and reading the two docs above, then report your
proposed v1 design (tile storage format + stable-ID scheme, animation handling, picker UI shape,
relay contract) before writing significant code — the storage format especially is worth a sanity
check before it's load-bearing.
