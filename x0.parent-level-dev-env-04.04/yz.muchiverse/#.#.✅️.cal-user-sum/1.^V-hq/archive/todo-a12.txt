══════════════════════════════════════════════════════════════════════
STALE — SUPERSEDED, kept for history only (2026-08-12, direct instruction
"remove the stale info or w/e"). Do not resume from this file.
- Phase A (db-ez) was explicitly dropped mid-session: "wanna just do
  db-hq. we may not need db-ez."
- Phase B (HQML CSS engine) is DONE, not a plan anymore: db-hq is real,
  built, wired, and RGB-refactored - see EVENTS-HQ-RGB-HANDOFF.md and
  !.khtpm-rgb-refactor.md for what actually shipped.
- Current real next-step list lives in EVENTS-HQ-RGB-HANDOFF.md §1
  ("Near-term: events-hq still needs real logic event-ez already has")
  and HAIKU_TASKS.md (bounded, ready-to-pick-up tasks).
══════════════════════════════════════════════════════════════════════

TODO — db-ez / db-hq / HQML CSS engine (started 2026-08-12)

Reference mockup: au11-hq/rpg-maker-database.html (SUPERSEDES the
earlier db-0000.html — same RPG Maker database shape: tab bar of 14
categories + sidebar id-list + settings panel, but with more real
widget detail: bordered "settings-block" panels with a floating
block-title label overlapping the top border, colored percentage-width
stat bars (MHP/MMP/ATK/etc, each its own fill color), grid-2col/3col
form layouts, a selected-highlight sidebar list, equip-slot dropdowns,
a traits box-list, a note textarea, bottom Apply/OK/Cancel buttons).
Reference design doc: au11-hq/HQML-DESIGN+PLANS.md (CSS layer for KHTPM).

WINDOW MODEL (direct instruction, 2026-08-12 - "db-ez is the separate
gl window. but db-hq will be a separate x11 window. get it?"):
- db-ez: separate GL window, same launch pattern as today's event-ez
  (setsid nohup sh -c open_event_ez.sh-style launcher, own process).
- db-hq: ALSO its own separate X11 window (own process/launch, NOT
  rendered inside the taskbar's hq popup) - just styled via the new
  CSS layer instead of plain grid chtpm. This is the real first proof
  of the HQML CSS engine, not a stub.

Direct instructions this session:
- db-ez: plain chtpm, placeholder tabs for ALL 14 db-0000.html categories
  (Actors/Classes/Skills/Items/Weapons/Armors/Enemies/Troops/States/
  Animations/Tilesets/Common Events/System/Types), only Common Events
  actually wired up to start — rest are inert tabs, filled in later.
- db-hq: NOT a placeholder — this is the real first proof-of-concept for
  the HQML CSS engine described in HQML-DESIGN+PLANS.md Phase 1. "the
  real point is hqml" (direct quote). Scope down to Common Events tab
  first, matching db-0000.html's visual pattern (tab bar + sidebar list +
  settings panel), styled via a real CSS file once the engine exists.

--- PHASE A: db-ez (plain chtpm, ships first, no new engine needed) ---
CORRECTION (direct instruction, 2026-08-12): "it doesn't have to be
tabs. just buttons and 'view section' that was just a crude mockup."
No tab-switching mechanism needed at all — this is just MORE MENU ROWS,
same pattern khtpm already uses for the session picker ("load" replaces
the open popup's row list in place via ktb_hq_open(s, 13), see
khtpm_taskbar_manager.c). db cell's menu becomes: 14 section rows
(Actors/Classes/.../Common Events/System/Types) -> clicking one calls
livedesk_build_db_menu()-style rebuild that replaces hq_menu[] with that
section's rows (or "(coming soon)" placeholder for the 13 unbuilt ones).
Exactly the existing hq_open/hq_menu reuse-across-cells model, one level
deeper. No new parser/layout plumbing required.

[ ] db menu top level: 14 section rows + back to close
[ ] Common Events row -> rebuilds hq_menu with list of existing
    common_events (read from sessions/<id>/common_events/) as its own
    rows; selecting one opens it (reuse open_event_ez.sh logic OR inline
    edit — decide which)
[ ] Other 13 section rows -> placeholder rows only ("(coming soon)"),
    just need to exist and not crash when clicked
[ ] Wire db cell's "db-ez" row (already exists,
    livedesk:open-common-events:<id> in khtpm_taskbar_manager.c) to
    launch db-ez.chtpm instead of / in addition to raw event-ez — CONFIRM
    with user whether db-ez replaces the current event-ez launch or is
    a new third option
[ ] Test: click db -> db-ez -> see tab bar, click Common Events, see list

--- PHASE B: HQML CSS engine kickoff (real work, not a stub) ---
Following HQML-DESIGN+PLANS.md's own Phase 1 plan, scoped to what
db-hq's Common Events section actually needs first (don't build the
full CSS spec up front — minimal subset, prove it end to end, then
grow). Own separate X11 window process (see WINDOW MODEL note above),
launched the same way db-ez/event-ez are (own binary or own khtpm
parser instance pointed at db-hq's own .chtpm+.css).
[ ] Design minimal CSS subset spec, driven by what
    rpg-maker-database.html actually uses: selectors (element, #id,
    .class, :hover), properties (color, background-color, font-family,
    font-size, padding, margin, border, border-radius,
    position/left/top/width/height — width needs PERCENTAGE support for
    stat-bar fills, not just px)
[ ] Floating block-title-over-border effect (settings-block's title
    label sits ON the top border line) — needs z-index/overlap, not
    just plain box layout; simplest approach is absolute-position the
    label over the block's own border, matching the mockup's own CSS
    exactly (position:absolute; top:-8px)
[ ] CSS parser in khtpm_strip_parser.c (or new file, e.g.
    khtpm_css_parser.c): tokenize + parse a .css file into a style rule
    list
[ ] Style application: match rules to chtpm elements by tag/id/class,
    cascade (element -> class -> id), produce computed style per element
[ ] X11 renderer changes: use computed style for colors/fonts/
    positioning/percentage-widths instead of the current fixed-grid
    draw logic — needs care not to break existing (unstyled) .chtpm
    files, which must keep rendering exactly as before (CSS is opt-in
    per HQML-DESIGN+PLANS.md's own risk table)
[ ] First proof: db-hq/dashboard.chtpm + dashboard.css, Common Events
    section only, visually matching rpg-maker-database.html's tab-bar +
    sidebar + bordered settings-block panels as the acceptance bar
    (stat bars can come later — Common Events doesn't need them; reuse
    that pattern once an actor/stat-bearing section gets built)
[ ] Wire db cell's "db-hq" row to launch this separate X11 window
    instead of the current no-op placeholder handler
    (khtpm_taskbar_manager.c livedesk:open-common-events-hq: branch)
[ ] Test: click db -> db-hq -> separate styled X11 window opens,
    visually distinct/nicer than db-ez's plain grid window

--- Open questions / decide before or during implementation ---
- Does db-ez's Common Events section replace the current plain event-ez
  launch, or do both stay reachable?
- CSS engine: confirm minimal-subset scope before writing the parser —
  don't try to match db-0000.html's full CSS file line for line, only
  what's needed for the Common Events tab's actual visual structure.

Status: not started — this file written before Phase A work began.
