# 🧭 Compaction Handoff — Aug 29 session

👋 **For you tomorrow, and for whichever AI picks this up.** This is the
plain-language summary of the tail end of a long session. Not a full
log — a "here's where we really are" doc, written so nobody (human or
AI) has to re-derive it from scratch or trust a stale claim again.

---

## ✅ What actually got built and shipped tonight

- 🖱️ **Real UI bug fixes in the events-hq Add-Command picker**: it's
  now built from real `.chtpm`-style elements + real CSS, not
  hand-drawn C. Fixed a real mouse-click bug (clicks were silently
  swallowed by the picker's own redraw undoing them) and a real
  nav-collision bug (the picker's own nav numbers were colliding with
  the background window's numbers, so pressing "8" in the picker
  could also select item 8 in the parent window). Both confirmed fixed
  live, not just "should work now."
- ⌨️ **House-wide two-step click**: a click now moves nav focus first;
  a second click on the same, already-focused thing activates it —
  matches how arrow-key nav + Enter already worked. Configurable via
  `hq_ui.pdl` (`click_two_step=0` reverts to old one-click-does-it-all).
- 🧩 **RPG Maker event commands — a LOT landed**: Message + Character
  commands (Task 1), 15 of ~19 Party/Actor commands (3 skipped
  honestly — they need a real string-SET VM op that doesn't exist yet,
  not faked), and **Flow Control is now fully complete** (Exit Event/
  Label/Jump to Label/Comment added tonight — turned out Loop/Break
  Loop/Conditional Branch already existed, an earlier planning doc had
  this wrong).
- 🧹 **Real infra cleanup**: moved the shared event-command ops
  (`mr_change_gold.c` etc.) out of one project's own folder into the
  real shared `events-hq/ops/` home (they were never muchi-pet-
  specific, just historically homed there first). Fixed 2 real orphan-
  process bugs (chat-hai's background loop, an events-hq manager) —
  both now have real liveness checks instead of just trusting a parent
  process to clean up after itself.
- 🤖 **3 background subagents** did #1/#2/#3 above in parallel while I
  worked on other things — all reviewed, tested, and merged by hand
  before pushing, not trusted blind.

**Everything above is committed and pushed to `main`.** Nothing is
sitting uncommitted or unpushed as of this doc.

---

## 😬 My real mistakes tonight — say them plainly, don't bury them

1. **I designed a whole "tile/map system" doc without checking for
   existing spec first.** Wrote real content (Options A/B/C for a 2D
   RPG-Maker-walk-around map) based on a gap-analysis doc's own
   framing — never checked whether a real spec already existed. It
   did. Multiple times over, in fact.
2. **When corrected once, I still didn't check thoroughly enough.**
   You asked "is there other documentation, maybe it's disparate?" —
   I found `PIECECRAFT_XYZ_DESIGN.md` (real, detailed, a whole
   chunked-voxel-world spec) and `CURSWORD-SOUL-VISION.md` §5, wrote a
   consolidation doc... and **still hadn't found `TILE-SYSTEM-
   DESIGN.md`**, sitting in the exact same folder, which turned out to
   be the actual primary doc for how a single tile works. Missed it
   twice before finding it.
3. **I claimed the palette picker was a stub.** It's not — real tab
   bar, real tileset chooser, real state round-trip
   (`rmmv_active.txt`, `set-rmmv-tab`/`set-rmmv-tileset` handlers) all
   genuinely exist and work. I was trusting a stale "fully static
   (title + [content])" comment/doc-status from Aug 27 instead of
   reading the actual current code. **Lesson, stated plainly: a doc
   saying "stub" is a claim, not a fact — check the code.**
4. **I almost mis-stated the drag-drop mechanism as unbuilt.** You
   caught this too (`101.drag-drop-test=ON🀄️?`) — the real desktop↔
   mutaclysm XDND transfer is built AND tested, not a future gap.

**The real, common thread in all 4 mistakes**: I kept treating "I
didn't find a doc" as "a doc doesn't exist," instead of "I should
search harder / ask before writing something new." The `1.^V-hq/`
folder being large (68 files) made this worse, not better — it's easy
to search shallowly and feel done.

---

## 🗺️ Where the REAL specs actually live now (post-cleanup)

- **`TILE-SYSTEM-DESIGN.md`** — the real, primary doc for how ONE
  placed tile works. A tile = a real desktop entity (same mechanism
  every pal uses), NOT a new canvas. Real, verified code already
  exists: autotile math (`tile_autotile.c`, pixel-verified against
  real RPG Maker assets), a real tileset registry
  (`tile_registry.c`/`tileset_registry.pdl`), configurable grid size.
  **What's still missing, concretely**: the "armed brush, click
  desktop to place" interaction itself (pick a tile, click a grid
  cell, a real tile-entity spawns there) — this is the real next
  buildable step, not a design question.
- **`PIECECRAFT_XYZ_DESIGN.md`** — the real spec for WHOLE chunked
  maps and Z-levels (a proper voxel world, not just one tile). Has
  your own real answers to its own open questions already recorded.
  Almost nothing built yet (Phase 0 partial, Phases 1-5 are plan only).
- **`#.DOX/drag-drop-how2.md`** — the real, working, TESTED X11 XDND
  drag-drop mechanism, desktop → Mutaclysm today (one direction only).
  `101.drag-drop-test=ON🀄️/` is the test HARNESS for this, not a
  second spec — don't confuse the two.
- **`MAPS-TILES-ZLEVELS-CONSOLIDATED-SPEC.md`** — NEW tonight, short,
  just a router pointing at the 3 docs above plus §4 below. Not a
  spec itself.
- **`GAME-READINESS-GAP-ANALYSIS-2026-08-27.md`** — its own gap #1
  (tile/map) is now formally retired, points here instead.

---

## 🆕 Genuinely new information from tonight — NOT written down
anywhere before this session, needs its own real design pass

These are real things you said tonight that don't exist in
`TILE-SYSTEM-DESIGN.md` OR `PIECECRAFT_XYZ_DESIGN.md` — they're the
actual gap now, not the tile mechanics themselves:

1. **🧘 Cursword drives Z-level navigation and map-view control,
   xelector-style.** `PIECECRAFT_XYZ_DESIGN.md` has an unanswered
   question ("what key moves the cursor vertically?") — you answered
   it tonight, but with a different actor than that doc assumed:
   cursword itself (your "SOUL" entity), not just a keypress. This is
   a real, new link between two docs that don't currently reference
   each other at all.
2. **🔀 The drag-drop mechanism needs to extend to piececraft-xyz.**
   Today's real, tested XDND only covers desktop↔Mutaclysm. You
   described tiles/maps moving between THREE surfaces (desktop,
   piececraft, mutaclysm), plus a real distinction between dragging
   ONE tile (small payload) vs. a WHOLE map (big, chunk-shaped
   payload) — neither the size question nor the piececraft extension
   has a real design yet.

**Real suggestion for tomorrow**: before touching any tile/map code,
spend 10-15 real minutes deciding these two things on paper (or just
talking it through) — they'll shape how `TILE-SYSTEM-DESIGN.md`'s own
next step (armed-brush placement) and any piececraft work relate to
each other, and it's cheap to decide now vs. expensive to redesign
after code exists.

---

## ❓ Real questions worth asking yourself (or me) before tomorrow's
session starts

1. Do you want the armed-brush placement UI (`TILE-SYSTEM-DESIGN.md`
   §6 step 6) built FIRST, before deciding the cursword/Z-level
   questions above — since single-tile placement doesn't actually
   need those answers yet? (My real recommendation: yes, it's fully
   unblocked and doesn't paint you into a corner either way.)
2. Is "a little game" still the near-term goal, using single tiles +
   events (fully unblocked today), with the whole chunked-voxel-
   piececraft vision as a real but LATER phase? Or has the ambition
   shifted toward piececraft being the actual near-term target?
3. Given tonight's pattern (missed docs, twice) — do you want a
   standing rule that any new design doc gets a real "did I search
   thoroughly" checklist before writing (e.g., grep the target
   keyword across the whole house, not just the one folder, before
   concluding "doesn't exist")? Worth adding to `SKILLS.md` if so.

---

## 📁 About the `1.^V-hq/` folder itself

68 files at top level before tonight's pass, only 12 in `archive/`.
Real risk you named: it's grown large enough that searching it
shallowly (which is what happened tonight, twice) feels complete when
it isn't. A real archival pass is a separate, following piece of work
— not done as part of writing THIS doc, to keep this one focused and
honest about what it actually covers. See whatever commit/note follows
this one for what got archived and why.
