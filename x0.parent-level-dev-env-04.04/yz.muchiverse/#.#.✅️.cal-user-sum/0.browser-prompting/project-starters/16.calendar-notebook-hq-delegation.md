# 📅📓 Calendar-Based Notebook App (HQ or Clock cell) — delegation map

**Your idea, organized:** collab-convo sidebar needs to be widenable + user input reliability
assured + forkable + dumpable; house needs real X11 copy-paste; a "notebook" app (HQ or clock
cell) that keeps a calendar of notes + a master note per month/year, calendar-shaped, with text
audit — one calendar per subject, OR one subject per day (skippable), prunable/compactable by a
basic agent.

**Real existing doc to check FIRST**: `au11-hq/15.clock-design.md` (24KB, dated 2026-08-13) —
you said yourself "this could even go under clock" — there's already a real clock-cell design doc
that may cover calendar UI shape. Don't re-derive calendar rendering from scratch if it's already
designed there.

## 🧩 This is really 3 separate features — split your prompts accordingly

1. **Sidebar widenable + forkable/dumpable + input reliability** — a chat-hai/collab-UI usability
   fix. Route through `ai-agents/3.chat-hai-openhai-harnecient-delegation.md`'s own delegation
   shape, not this file.
2. **X11 copy-paste** — a genuine, house-wide input-handling gap (PRIMARY/CLIPBOARD selection
   support in the `khtpm_*` family's Xlib event loop). This is infrastructure, not app-specific —
   worth its own standalone prompt, reusable by every future text-input feature (including this
   notebook app).
3. **The notebook app itself** — the calendar-shaped notes UI. This is the one this file's
   delegation map below covers.

## 📎 What to attach (for the notebook app specifically)

| Slot | File |
|---|---|
| Context #1 | `1.platform-primer-ALWAYS-ATTACH.md` |
| Context #2 | `au11-hq/15.clock-design.md` — check for reusable calendar-rendering design first |
| Code | Clock cell's own current manager/layout (whatever `15.clock-design.md` points at as the real implementation) |

## 🤔 Decide before prompting: per-subject-calendar vs per-day-subject

You named both shapes yourself without picking one — same "decide first" caution as the palettes
topic (file 11). A browser AI given both options will guess. Pick one for the FIRST build; the
other can be a later mode if it still seems worth it once you're using the first one.

## ✍️ Prompt shape (notebook app, after picking a shape above)

```
CONTEXT: [platform-primer + 15.clock-design.md]
CODE: [clock cell's current manager/layout]
PROMPT:
  "Add a notebook feature to the clock cell: [per-subject calendar / per-day subject - STATE
  WHICH]. Calendar-shaped note view, one master note per month/year, full text audit trail
  (append-only, per the house's real ledger convention). A basic agent should be able to
  prune/compact old entries later - don't build that pruning agent yet, just leave the data shape
  friendly to it (plain text, dated, one entry per line)."
```

## ✍️ Prompt shape (X11 copy-paste, separate prompt, infrastructure-level)

```
CONTEXT: [platform-primer]
CODE: [the khtpm_* family's own Xlib event loop, e.g. khtpm_strip_parser.c's KeyPress handling
       as a REFERENCE for where selection/clipboard events would need to hook in]
PROMPT:
  "Add PRIMARY/CLIPBOARD selection support (X11 copy-paste) to the khtpm_* family's cli_io text-
  input widgets. This is infrastructure other features (notebook, chat-hai) will depend on -
  keep it in the shared css/layout parser layer if there's an obvious shared hook point, not
  duplicated per-app."
```
