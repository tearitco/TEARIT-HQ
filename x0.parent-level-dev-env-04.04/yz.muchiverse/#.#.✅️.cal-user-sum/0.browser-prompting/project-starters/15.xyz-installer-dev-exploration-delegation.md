# 📦🧙 xyz-installer-dev — Exploration + Wizard-Style Installer — delegation map

**Real location confirmed:** `x0.parent-level-dev-env-04.04/xyz-installer-dev/`

**What's actually there (confirmed 2026-08-18, so you have a real starting map instead of
guessing):**
```
xyz-installer-dev/
├── README.md                        ← START HERE, read this one yourself first
├── pointers.pdl                     ← points the installer at its real source tree
│                                       (dev_tree = 44.xyz❤️‍🔥️00.10 - note: 00.10, a DIFFERENT
│                                        version number than the 00.17 this session worked in!)
├── user-quick.txt
├── #.wussp-in.txt
├── xyzos-starter-install.sh
├── xyz-official-documentation/
└── dev-doc/
    ├── 01.versioning-with-gitlet🌳🪵️.md
    ├── 02.rollout-channels-alpha-beta-stable.md
    ├── 03.hardcoded-path-fragility-and-portability.md
    ├── 04.harnecient-fresh-install-design.md
    └── ha-install-oc.md
```

**⚠️ Real thing worth checking before you go further, human-to-human**: `pointers.pdl` points at
`44.xyz❤️‍🔥️00.10` as the dev tree, but this whole session's taskbar work happened in
`44.xyz❤️‍🔥️00.17` — a different version snapshot. Confirm which version the installer is actually
meant to package before doing more work here; that mismatch alone might be the real blocker,
not a missing feature.

## 📎 Top docs to show the agent (no deep research needed — these 4 numbered docs already exist
FOR exactly this)

1. **`xyz-installer-dev/README.md`** — read yourself first, it's short.
2. **`xyz-installer-dev/dev-doc/04.harnecient-fresh-install-design.md`** — closest existing doc
   to your "wizard guiding user through install location/name" idea.
3. **`xyz-installer-dev/dev-doc/03.hardcoded-path-fragility-and-portability.md`** — directly
   relevant since the house is FULL of hardcoded, deeply-nested paths (you've seen this all
   session) — an installer's #1 real risk.
4. **`xyz-installer-dev/pointers.pdl`** — attach as CODE, small enough, shows the real
   version-pointer mechanism.

## ✍️ Prompt shape (exploration pass, not a build yet)

```
CONTEXT: [README.md + 04.harnecient-fresh-install-design.md + 03.hardcoded-path-fragility.md]
CODE: [pointers.pdl]
PROMPT:
  "Summarize the current real state of this installer project against my goal: a wizard-style
  installer (install location, name, unpacking selected projects). What already exists toward
  that goal vs. what's missing? Also flag: pointers.pdl points at version 00.10 - is that still
  the intended package target, or stale?"
```

Use this as a genuine FIRST prompt (status-summary, not build) — you said yourself you don't yet
have a grasp on what's there, so let the first browser-AI round be discovery, not construction.
