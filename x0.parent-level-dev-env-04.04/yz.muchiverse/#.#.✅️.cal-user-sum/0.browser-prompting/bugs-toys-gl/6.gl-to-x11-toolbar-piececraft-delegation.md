# 🖼️➡️🪟 GL Windows → In-House X11 Toolbar (piececraft-xyz) — delegation map

**Goal:** switch GL-rendered windows (piececraft-xyz at `@.apps/piececraft-xyz/`) to use the
in-house X11 taskbar instead of their own separate GL window management.

**⚠️ Real, confirmed context first**: this is explicitly a KNOWN, TRACKED, still-open thread —
`au11-hq/legacy-shared-fix.md` reports 3 of 16 legacy GL projects (mutaclysm, piececraft-xyz,
my-chara-txt) already converted to a shared `x11_mirror.c` display-shim binary as of last count.
**Piececraft-xyz may ALREADY be partially done** — verify current status before treating this as
greenfield.

## 📎 What to attach

| Slot | File |
|---|---|
| Context #1 | `au11-hq/legacy-shared-fix.md` — real current migration status (which of the 16 are done, the shared `x11_mirror.c` shape) |
| Context #2 | `@.apps/piececraft-xyz/PIECECRAFT_XYZ_DESIGN.md` — the project's own design doc |
| Code | `&.widgits/_shared-lib/`'s real `x11_mirror.c` (the shared shim other converted projects already use) — attach as reference/CODE, not context |

## ✍️ Prompt shape

```
CONTEXT: [legacy-shared-fix.md + PIECECRAFT_XYZ_DESIGN.md]
CODE: [x11_mirror.c as the reference shape, + piececraft-xyz's own current GL window code]
PROMPT:
  "First: confirm from legacy-shared-fix.md whether piececraft-xyz is one of the 3 already-
  converted projects. If YES, describe what's left/broken. If NO, port piececraft-xyz's own GL
  window management onto x11_mirror.c's real shared shape - don't invent a new pattern, copy the
  convention the 3 already-done projects use."
```
