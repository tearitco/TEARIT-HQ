# 🐛🕹️🧸 Remaining Bugs + Joystick + More Toys — delegation map

**Goal:** clear out leftover bugs from prior sessions (incl. the "9 bugs" batch), finish the
joystick input work, add more taskbar "toys" cell entries.

## 📎 What to attach

| Slot | File |
|---|---|
| Context #1 | `au11-hq/legacy-shared-fix.md` — real, current status of the shared-binary consolidation + GL migration (13 of 16 legacy projects still not on `x11_mirror.c`) + mutaclysm's own deferred camera/3D bug history |
| Context #2 | The specific project's own real bug-tracking file if one exists (check for a `dox/00-HANDOFF.md` or `PITFALLS*.txt` in that project's own dir FIRST — don't assume `legacy-shared-fix.md` covers every project) |
| Code | The one file the specific bug lives in |

**For joystick specifically**: TPMOS's own real `joystick_input.c` pattern (fork-and-return, never
`waitpid`/`pthread_join` on it — Pitfall #34 in TPMOS's own pitfalls doc) is the reference shape.
Attach that real file as your CODE context if porting it, not a from-scratch design.

**For "adding more toys"**: check `au11-hq/TASKBAR-MENU-ARCHITECTURE.md`'s own "toys cell" section
— confirm whether toys are PDL-driven or C-hardcoded before assuming a config edit is enough.

## ✍️ Prompt shape (bug fix)

```
CONTEXT: [legacy-shared-fix.md, or the project's own dox/00-HANDOFF.md]
CODE: [the one file with the bug]
PROMPT:
  "Bug: [describe exact symptom + repro]. This project uses the legacy chtpm_parser_pal.c family
  (PAL-VM/text-grid), NOT the khtpm_* family — don't apply khtpm-specific fixes. Find root cause
  before patching around it."
```

## ✍️ Prompt shape (joystick)

```
CONTEXT: [TPMOS's real system/joystick_input.c, as CODE not context — it's the reference impl]
CODE: [target project's own system/ dir, or wherever input capture lives]
PROMPT:
  "Port joystick_input.c's real pattern (fork-and-return, never joined) into this project. Match
  its own real state-file-driven input capture shape — no new relay/dispatch invention."
```
