# 🌐📡 Networking Apps → House-Friendly + Populate `network` Cell — delegation map

**CORRECTED 2026-08-18** — original version of this file wrongly said no networking apps exist.
They do: `041.pal-chain⛓️/`, `041.pal-forum👥️/`, `044.pal-chat-irc👥️+2/` (all at house root). All
three ALREADY have a real `pal/`, `ops/`, `net/`, `button.sh`, and a `phase2-module-split-report.txt`
— meaning some house-alignment work has already happened, this is NOT a from-scratch conversion.
**Goal, corrected**: verify how far that phase-2 split actually got, THEN finish wiring into the
taskbar's `network` cell (`strip_btn_9_label = network` in `livedesk_taskbar.pdl` — still just a
label, no menu wired to it yet, that part of the original claim was accurate) and decide whether
they live under a new `13.network/` grouping dir or stay at their current house-root locations.

## 🔎 First prompt should be discovery, not conversion

Given real prior work already exists (`phase2-module-split-report.txt` in each), don't let a
browser AI start converting blind — first ask it to read that report + summarize current real
status per app, same "discovery before construction" discipline as the xyz-installer-dev topic
(`project-starters/15.`).

## 🧠 What "house-friendly" concretely means here

- PIECE → MODULE → OS shape (see `1.platform-primer-ALWAYS-ATTACH.md`).
- File-backed state, one-writer-rule, marker-pulse redraw signaling.
- A `*_manager.c` that's thin (delegates to ops), a `.chtpm` layout using `khtpm_*`-family tags
  if it's taskbar-launched (no flexbox/descendant selectors).
- Menu wiring goes through the taskbar's real dispatch string convention
  (`"livedesk:open-X"` → `strcmp` branch in `ktb_hq_activate()`), NOT a raw shell command baked
  into a menu builder (silently no-ops on relative paths — real, already-hit house pitfall).

## 📎 What to attach

| Slot | File |
|---|---|
| Context #1 | `1.platform-primer-ALWAYS-ATTACH.md` |
| Context #2 | `au11-hq/TASKBAR-MENU-ARCHITECTURE.md` — exact recipe for wiring a new cell's menu correctly |
| Code | ONE of the three apps' `phase2-module-split-report.txt`, as the FIRST prompt's only code attachment |

## ✍️ Prompt shape #1 — discovery (do this first, per app)

```
CONTEXT: [platform-primer + TASKBAR-MENU-ARCHITECTURE.md]
CODE: [041.pal-chain / 041.pal-forum / 044.pal-chat-irc's own phase2-module-split-report.txt]
PROMPT:
  "Summarize this networking app's real current status against house convention (PIECE/MODULE/OS,
  file-backed state, thin manager). What's already done per this report vs. still needed before
  it could wire into the taskbar's `network` cell (strip_btn_9)?"
```

## ✍️ Prompt shape #2 — the actual taskbar wiring (after discovery confirms readiness)

```
CONTEXT: [platform-primer + TASKBAR-MENU-ARCHITECTURE.md]
CODE: [that app's own manager/ops entry point]
PROMPT:
  "Wire this app into the taskbar's `network` cell (strip_btn_9) using the real 'livedesk:open-X'
  dispatch-string convention from TASKBAR-MENU-ARCHITECTURE.md, NOT a raw shell command. Don't
  invent a new relay/dispatch mechanism - reuse the existing one."
```

**Do this in small bites** — one networking capability (e.g. "chat over LAN") per browser prompt,
not the whole networking suite at once, given the 1-2-page-of-code return limit.
